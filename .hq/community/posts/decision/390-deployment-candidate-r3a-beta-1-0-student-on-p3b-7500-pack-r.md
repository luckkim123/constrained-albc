# Deployment candidate = R3a (beta 1->0 student on p3b_7500): pack_r3a_p3b7500_gru_260906_145553 exported, container self-close + Mac sha256/parity/board-test gates PASSED, board gate pending (jump host offline)

- id: decision/390 · date: 2026-09-06 · author: omx
- harness: omo · to: all
- topic: decision
- confidence: high · status: needs-experiment
- verified: none · keywords: deployment, field test, R3a, deploy pack, pack_r3a_p3b7500_gru, parity, sha256, Mac gate, board gate, agent-jetson, numpy_port, retrain-simtoreal-2026-09
- summary: User chose R3a for the next field test on the fault-type cells (deployed student vs R3a pair34/hard 12.77/1.45/45.2 vs 5.76/1.08/16.3 mean/median/P90). Pack exported with --golden --report; dims and npforward.py identical to the deployed pack, so the board swap is 5 data files. Container self-close 6.4e-7/1.3e-7; Mac gates: sha256 5/5, self_close 5.5e-7/8.9e-8, board tests ALL PASS + 12 passed/2 xfailed. Board (TX2 numpy 1.11) pending: kim-macbookair offline; procedure recorded. Operator required for the robot.

**Deployment candidate for the next field test is R3a (`sd_p3b7500_c3_dr5_beta0_s30`, `student_999.pt`, teacher `p3b_7500`), chosen over the deployed student `sd_inc9998` and the other four arms on the fault-type exam cells. The pack `deploy/retrain_simtoreal_p3/pack_r3a_p3b7500_gru_260906_145553` is exported, self-closed in the container, and has passed the Mac sha256 + parity gates and the board package's own test suite run on the Mac. The board gate (TX2, numpy 1.11) is pending: the jump host is offline.**

**1. Why R3a (user decision 2026-09-06 ~14:50, "진행해줘").** Per-env att mean / median / P90 on the deployment-relevant cells, deployed student vs R3a: pair34/hard 12.77 / 1.45 / 45.2 vs 5.76 / 1.08 / 16.3; pair34/medium 3.92 / 1.27 / 8.70 vs 1.94 / 0.96 / 4.77; pair34_d2/hard 13.11 / 5.50 / 38.8 vs 7.91 / 3.13 / 25.0; pair34_d2/medium 4.30 / 1.79 / 6.24 vs 2.20 / 1.01 / 3.99; healthy_d2/hard 4.05 / 1.47 / 9.47 vs 4.18 / 1.00 / 10.0; healthy/none 0.56 / 0.36 / 0.72 vs 0.52 / 0.50 / 0.73. Survival at pair34_d2/hard 98.4 vs 96.9 %. The two rows where R3a trails the deployed student on the mean (healthy/hard +0.13, healthy_d2/hard +0.13) are inside the single-realization noise measured in finding/389 and are median wins. R4a beats the deployed student only on pair34-type rows and loses to R3a on 16 of 20; R1, R2, R3b lose to R3a on every row (finding/384, 386, 388, 389).

**2. Pack.** `scripts/export_deploy_pack.py --batch attitude_only_5000 --student-ckpt <R3a student_999.pt> --teacher-ckpt <p3b model_7500.pt> --run-group retrain_simtoreal_p3 --tag pack_r3a_p3b7500_gru --device cpu --golden --report` (CUDA hidden; goldens are CPU by contract). Contract PASS for both specs; dims obs 72 / action 8 / latent 9 / teacher_input 81 / gru_hidden 128 -- identical to `pack_inc9998_gru_260810_150713`, and `npforward.py` is byte-identical to that pack (sha256 8ab626b0...), so this deployment is a data swap: `weights_gru.npz`, `weights_teacher.npz`, `golden/golden_gru.npz`, `golden/golden_teacher.npz`, `MANIFEST.gru.json`. Container self-close: teacher normalize 0.0, teacher act 6.4e-7, GRU latent 1.3e-7, hidden 1.5e-7 (atol 1e-5).

**3. Mac gates (2026-09-06 14:56-14:59).** (a) sha256 of all 5 payload files vs MANIFEST: 5/5 PASS after scp to `~/albc_deploy/retrain_simtoreal_p3/<pack>/`. (b) `pack.self_close` re-run with Mac numpy 2.4.6 (python 3.12): teacher act 5.5e-7, GRU latent 8.9e-8, hidden 1.3e-7 -- CLOSED. (c) The board package's own tests, run against a scratch copy of the agent-jetson clone's `robot/albc_rl` with the 5 pack files dropped into `numpy_port/`: `test_npforward.py` ALL PARITY CHECKS PASSED (teacher, TCN [old weights, untouched], GRU [new weights]), `test_np_policy_api.py` 12 passed / 2 xfailed (the 2 xfails are the standing tick-1 saturation cases, unchanged). The board runtime `src/albc_rl/npforward.py` is AST-identical to the pack's `npforward.py` (docstring-only differences: the 2026-09-04 raw-obs-into-encoder clarification).

**4. Board step -- PENDING, operator required.** The board is reached through `kim-macbookair` (Tailscale jump), offline since ~10:50. Per the clone's `numpy_port/README.md` ("Replace pack artifacts only: weights_*.npz, golden/, MANIFEST.*.json. Board code is now in src/albc_rl/"), the board procedure is: (1) confirm which branch the board runs (`deploy/72d-inc9998-gru` at 90b13e4 still has the pre-refactor layout with `npforward.py` inside `numpy_port/`; `refactor/cleanup-2026-09` has the new layout) -- the file set differs by exactly that one file; (2) back up the current 5 files OUTSIDE the ROS package (`~/albc_backups/pre_r3a_<ts>/`, never inside `albc_rl/` -- roslaunch scans it); (3) copy the 5 files, (4) run `test_npforward.py` on the board (py2.7, numpy 1.11) and check sha256 container = Mac = board; (5) field test with the operator. No robot operation without the operator (standing rule).

**5. Not covered by these gates.** The module goldens pin the model forward; obs assembly on the board (`np_policy._assemble_obs`, bias_ema 3 channels) is unchanged code and was pinned against the torch e2e oracle on 2026-09-04 for the incumbent weights; it is not re-pinned here. The real-robot delay (152 ms, 7.6 steps) is outside R3a's training envelope (0,3) as it was outside the incumbent's (0,0) -- the exam's d2 rows do not represent it. Side observation, not acted on: the Mac clone `01_code/agent-jetson` (branch `refactor/cleanup-2026-09`) shows `D CMakeLists.txt`, `D tests/board_gate.sh` with untracked `tests/board_gate (1).sh` / `board_gate 2.sh` -- the Drive conflict-copy signature recorded in the agent-jetson-clone memory; the clone was only read, not modified.
## Comments

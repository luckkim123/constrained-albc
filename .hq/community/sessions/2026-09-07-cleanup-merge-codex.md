VERDICT: LAND

No confirmed merge-resolution defect in the supplied executable code would ship wrong p5-exam or student behavior. The remaining concerns are either non-executable stale wording or dependencies/callers that are outside the supplied materials.

## 1. The nine conflict hunks

1. **config constraint-list name/count — REFUTED.** The resolution takes cleanup's `_MAIN_CONSTRAINT_TERMS` name and p5's correct "Probabilistic (4)" count; the list contains four probabilistic entries followed by six average entries, and the config consumes the renamed symbol (`merged/config.py:51`, `merged/config.py:54`, `merged/config.py:55`, `merged/config.py:56`, `merged/config.py:62`, `merged/config.py:63`, `merged/config.py:73`, `merged/config.py:699`).
2. **albc pre-physics OU/Fz split — REFUTED.** The deleted OU step is absent while p5's enabled-gated `_step_fz_disturbance()` call remains (`merged/albc_env.py:721`, `merged/albc_env.py:742`, `merged/albc_env.py:745`, `merged/albc_env.py:746`).
3. **albc command resampling — REFUTED.** The inert cleanup-deleted command-scale multipliers are gone, while p5's heading-target draw remains `uniform_(yaw_lo, yaw_hi)` rather than the cleanup side's obsolete `yaw_max` expression (`merged/albc_env.py:841`, `merged/albc_env.py:843`, `merged/albc_env.py:845`, `merged/albc_env.py:846`).
4. **albc OU/Fz method block — REFUTED.** The OU method is absent, but all three Fz lifecycle methods remain and link reset -> draw -> step/expired redraw (`merged/albc_env.py:966`, `merged/albc_env.py:984`, `merged/albc_env.py:986`, `merged/albc_env.py:999`, `merged/albc_env.py:1004`).
5. **albc mid-episode logging — REFUTED.** The OU-current log block is absent and both p5 disturbance log lines survive behind `disturbance.enable` (`merged/albc_env.py:1468`, `merged/albc_env.py:1472`, `merged/albc_env.py:1476`, `merged/albc_env.py:1477`).
6. **albc non-DR reset — REFUTED.** The deleted OU-base assignment is absent, while both p5 reset calls survive before the early return (`merged/albc_env.py:1701`, `merged/albc_env.py:1704`, `merged/albc_env.py:1709`, `merged/albc_env.py:1710`, `merged/albc_env.py:1711`). The DR-enabled path also retains the Fz reset (`merged/albc_env.py:1783`, `merged/albc_env.py:1785`).
7. **eval robustness initial command — REFUTED.** The resolution latches post-reset yaw once, writes it as the yaw heading target, and retains cleanup's `_vel_cmd_lin` capability guard (`merged/eval.py:1896`, `merged/eval.py:1903`, `merged/eval.py:1905`, `merged/eval.py:1911`, `merged/eval.py:1914`, `merged/eval.py:1915`, `merged/eval.py:1916`).
8. **eval robustness loop command — REFUTED.** Every loop iteration reasserts the same latched `_hold_yaw`, not live yaw, while retaining the guarded linear-command write (`merged/eval.py:1926`, `merged/eval.py:1929`, `merged/eval.py:1932`, `merged/eval.py:1933`, `merged/eval.py:1934`).
9. **eval switching yaw/linear write — REFUTED.** The yaw slot is p5's world-frame heading target `0.0`; cleanup's explanation for the deliberately unguarded linear writes remains, and `run_segmented` establishes the required capability before this helper is called (`merged/eval.py:2305`, `merged/eval.py:2308`, `merged/eval.py:2309`, `merged/eval.py:2313`, `merged/eval.py:2497`, `merged/eval.py:2503`).

## 2. Auto-merged deleted/renamed names

- **`_cmd_lin_scale`, `_cmd_att_scale`, `_cmd_yaw_scale` — REFUTED.** Cleanup explicitly deletes the three allocations and both sampling reads (`diff_vs_exp_parent.patch` hunks `@@ -479,16 +432,8 @@` and `@@ -915,12 +842,8 @@`), and there are no exact references in the merged files; the sampling implementation directly uses `att_max` and `(yaw_lo, yaw_hi)` (`merged/albc_env.py:841`, `merged/albc_env.py:846`). The prose saying ranges use `cmd_*_scale` is stale but is not an identifier read (`merged/albc_env.py:821`, `merged/albc_env.py:822`).
- **`ou_enable`, `ou_theta`, `ou_sigma`, `_step_ocean_current_ou` — REFUTED.** Cleanup explicitly deletes the config fields and the step method (`diff_vs_exp_parent.patch` hunks `@@ -616,13 +606,6 @@` and `@@ -1036,34 +959,6 @@`), and no exact merged references remain; fixed-per-episode ocean current is still randomized only at reset/mid-episode DR entry points (`merged/albc_env.py:1748`, `merged/albc_env.py:1749`, `merged/albc_env.py:1869`, `merged/albc_env.py:1870`).
- **`_ou_base_current` — REFUTED as a runtime dangling reference.** Its sole exact occurrence is a stale comment; no executable read/write remains (`merged/albc_env.py:395`, `merged/albc_env.py:398`, `merged/albc_env.py:401`).
- **`yaw_rate_cmd_range`, `_yaw_rate_err` — REFUTED.** Neither remains. Their live replacements are `yaw_cmd_range` and `_yaw_err`, with heading error wrapped through `atan2(sin, cos)` (`merged/config.py:577`, `merged/config.py:579`, `merged/albc_env.py:405`, `merged/albc_env.py:1299`, `merged/albc_env.py:1300`).
- **`_vel_cmd_lin` — REFUTED.** Every remaining occurrence belongs to one of three coherent groups: static capability detection plus guarded writes (`merged/eval.py:882`, `merged/eval.py:886`, `merged/eval.py:911`, `merged/eval.py:914`); robustness guarded zeroing (`merged/eval.py:1909`, `merged/eval.py:1915`, `merged/eval.py:1916`, `merged/eval.py:1933`, `merged/eval.py:1934`); or segmented's intentionally unguarded cascade writes after its setup-time refusal (`merged/eval.py:2309`, `merged/eval.py:2313`, `merged/eval.py:2315`, `merged/eval.py:2489`, `merged/eval.py:2497`, `merged/eval.py:2503`). There is no `_vel_cmd_lin` reference in the merged main environment itself (`merged/albc_env.py:93`, `merged/albc_env.py:97`, `merged/albc_env.py:98`).
- **`_FULL_DOF_CONSTRAINT_TERMS` / `_MAIN_CONSTRAINT_TERMS` — REFUTED.** The rename is explicit (`diff_vs_exp_parent.patch` hunk `@@ -47,13 +46,12 @@`); the former has no merged occurrence, and the latter occurs exactly at its definition and consumer (`merged/config.py:54`, `merged/config.py:699`).
- **Koopman / `use_marine_feature_obs` — REFUTED.** Cleanup deletes the fields/materializers and the environment-side width/observation paths (`diff_vs_exp_parent.patch` hunks `@@ -761,22 +744,6 @@`, `@@ -869,74 +836,6 @@`, `@@ -133,16 +127,6 @@`, and `@@ -1364,56 +1259,6 @@`), and no exact `koopman`, `Koopman`, or `use_marine_feature_obs` reference remains in the three merged files. The surviving observation materializers and runtime width calculation cover only bias EMA, extra policy observations, and privileged-fault observations (`merged/albc_env.py:31`, `merged/albc_env.py:35`, `merged/albc_env.py:119`, `merged/albc_env.py:134`, `merged/albc_env.py:213`, `merged/albc_env.py:229`).

## 3. `eval.py` heading-target and mode audit

- **`run_robustness_eval` — REFUTED.** The throwaway step completes the reset before `_hold_yaw` is read; `_hold_yaw` is cloned once and then re-used on every evaluation step (`merged/eval.py:1896`, `merged/eval.py:1903`, `merged/eval.py:1911`, `merged/eval.py:1912`, `merged/eval.py:1929`, `merged/eval.py:1932`). This preserves “hold the reset heading” instead of chasing live yaw.
- **static trajectory — REFUTED.** The CLI exposes heading amplitude (with the old flag only as an alias), trajectory construction receives `yaw_amp`, and the yaw column receives that trajectory as a heading target; measured yaw is wrapped and stored for heading-error metrics (`merged/eval.py:169`, `merged/eval.py:177`, `merged/eval.py:1559`, `merged/eval.py:1564`, `merged/eval.py:908`, `merged/eval.py:910`, `merged/eval.py:963`, `merged/eval.py:967`, `merged/eval.py:1014`, `merged/eval.py:1021`).
- **static `--flat-target` / zero-command path — REFUTED.** It turns the target trajectory, including yaw, into zero (`merged/eval.py:1565`, `merged/eval.py:1569`). For the supplied main environment this is also a hold target because every rollout reset uses identity orientation before command sampling (`merged/albc_env.py:1787`, `merged/albc_env.py:1790`, `merged/albc_env.py:1791`, `merged/albc_env.py:1824`, `merged/albc_env.py:1825`).
- **`run_switching_eval` — REFUTED.** The removed yaw-rate outer loop is not reconstructed elsewhere: both CLI knobs are ignored, the yaw column is the constant world-heading target zero, and the compatibility array contains the command actually sent (`merged/eval.py:2282`, `merged/eval.py:2289`, `merged/eval.py:2294`, `merged/eval.py:2296`, `merged/eval.py:2305`, `merged/eval.py:2308`, `merged/eval.py:2387`).
- **deprecation/refusal consistency — REFUTED.** The parser labels both yaw-rate controller knobs deprecated and ignored (`merged/eval.py:359`, `merged/eval.py:361`); a capable linear-command environment gets a warning only when non-default deprecated values were supplied (`merged/eval.py:2288`, `merged/eval.py:2289`). The current attitude-only task cannot honestly run the segmented cascade, so `run_segmented` refuses before the unguarded writes (`merged/eval.py:2489`, `merged/eval.py:2497`, `merged/eval.py:2503`). These are complementary conditions, not contradictory behavior.

## 4. `commit_4f04a73_review_fixes.patch`

- **`_is_run_dir` scan behavior — REFUTED for the caller visible through the test.** The change catches only `RuntimeError` from manifest parsing, logs it, then deliberately falls back to the legacy `train`-entry predicate; the added `find_runs` regression checks that a corrupt sibling does not hide a good run (`commit_4f04a73_review_fixes.patch` hunk `@@ -167,7 +167,14 @@`; hunk `@@ -648,6 +648,16 @@`). A corrupt directory that also has a `train` entry can consequently remain discoverable as a legacy run; that matches the hunk's “treats the directory like a legacy one” comment, although the warning's word “skipped” refers only imprecisely to the bad manifest (`commit_4f04a73_review_fixes.patch` hunk `@@ -167,7 +167,14 @@`).
- **named-run callers — CANNOT-DECIDE.** The hunk claims `resolve_run` and `emit_run_manifest` retain fail-loud behavior, but neither caller body is present in the supplied patch, so it is impossible here to verify that they call `_read_manifest_if_present` directly rather than `_is_run_dir` (`commit_4f04a73_review_fixes.patch` hunk `@@ -167,7 +167,14 @@`).
- **`_should_log` — REFUTED at the changed definition.** Restoring `@property` makes attribute-style reads evaluate `log_dir is not None and not disable_logs`; without it, those reads see an always-truthy bound method (`commit_4f04a73_review_fixes.patch` hunk `@@ -122,6 +122,7 @@`). That is the intended behavioral correction, not an extra side effect.
- **the three `_should_log` call sites — CANNOT-DECIDE.** Their bodies are absent. If they are the attribute-style reads described by the commit, they now receive a boolean as intended; the materials cannot independently exclude a parenthesized `self._should_log()` call, which would be incompatible with a property (`commit_4f04a73_review_fixes.patch` hunk `@@ -122,6 +122,7 @@`).

## 5. Other p5-exam/student-stage differences from `d1fb67d`

- **Additional confirmed runtime/import defect — REFUTED.** Within the supplied files, p5's disturbance is fully connected from config to reset, per-step update, wrench application, and logging (`merged/config.py:270`, `merged/config.py:278`, `merged/config.py:451`, `merged/config.py:467`, `merged/config.py:643`, `merged/albc_env.py:966`, `merged/albc_env.py:999`, `merged/albc_env.py:1071`, `merged/albc_env.py:1074`, `merged/albc_env.py:1472`, `merged/albc_env.py:1477`, `merged/albc_env.py:1783`, `merged/albc_env.py:1785`). The heading command is likewise defined, sampled, and errored consistently as a target angle (`merged/config.py:573`, `merged/config.py:577`, `merged/albc_env.py:820`, `merged/albc_env.py:846`, `merged/albc_env.py:1294`, `merged/albc_env.py:1302`).
- **Non-executable residue — REFUTED as a run/import defect.** Several comments/docstrings still say “yaw rate,” “5 probabilistic + 5 average,” command scales, or OU after the executable features changed (`merged/config.py:8`, `merged/config.py:476`, `merged/config.py:480`, `merged/config.py:697`, `merged/albc_env.py:398`, `merged/albc_env.py:821`, `merged/albc_env.py:858`, `merged/albc_env.py:1469`, `merged/eval.py:786`, `merged/eval.py:883`). They are inaccurate documentation but cannot alter tonight's execution.
- **Relocated imports — CANNOT-DECIDE from this partial tree.** The merged files now import algorithm/student/logging symbols from `constrained_albc.algorithms.*` (`merged/albc_env.py:28`, `merged/albc_env.py:29`, `merged/eval.py:450`, `merged/eval.py:454`, `merged/eval.py:1109`, `merged/eval.py:1485`). Their destination modules are not supplied, so import availability and compatibility cannot be executed or inspected here.
- **External eval-helper contract — CANNOT-DECIDE from this partial tree.** Static eval now requires `_eval_dr.trajectory.YAW_AMP`, a `yaw_amp=` builder parameter, heading-aware metric keys, and plotting/serialization consumers of `yaw` (`merged/eval.py:43`, `merged/eval.py:48`, `merged/eval.py:1559`, `merged/eval.py:1564`, `merged/eval.py:1757`, `merged/eval.py:1760`, `merged/eval.py:1803`). Those helper files are absent.

## Could not decide without the repository

- Whether `resolve_run` and `emit_run_manifest` bypass `_is_run_dir` and therefore still raise on a corrupt explicitly named manifest (`commit_4f04a73_review_fixes.patch` hunk `@@ -167,7 +167,14 @@`).
- Whether all three `_should_log` consumers use property access rather than call syntax (`commit_4f04a73_review_fixes.patch` hunk `@@ -122,6 +122,7 @@`).
- Whether every relocated `constrained_albc.algorithms.*` module/export exists at merge tip (`merged/albc_env.py:28`, `merged/albc_env.py:29`, `merged/eval.py:450`, `merged/eval.py:454`).
- Whether `_eval_dr/trajectory.py`, `_eval_dr/metrics.py`, `eval_plots.py`, and `eval_serialize.py` implement the heading-key/signature contract consumed here (`merged/eval.py:37`, `merged/eval.py:48`, `merged/eval.py:427`, `merged/eval.py:436`).
- Whether checkpoint/Hydra readers outside these files tolerate the intentional removal of old `ou_*`, terrain, Koopman/marine-feature, and `normalize_value` fields; the supplied review patch records that serialized-reader surface but does not include the readers (`commit_4f04a73_review_fixes.patch` hunk `@@ -52,13 +52,31 @@`).

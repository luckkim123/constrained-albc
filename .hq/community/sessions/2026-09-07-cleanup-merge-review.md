# 2026-09-07 — review of `cleanup/2026-09` (ponytail worktree) for merge into the p5 trunk (omo degraded-mode worker record)

- session: ksm-mac, Claude Fable 5.1; operator ask "ponytail worktree 검토 + main 병합, 시점은 큐 종료 후 또는 지금 큐 종료 후 먼저"
- subject: Orca workspace `/root/orca/workspaces/workspace/ponytail/` — nested clone `constrained-albc` branch `cleanup/2026-09` (15 commits, base 98dd2f2 → tip ff47492, 199 files ±5.9k: WP0 unpin/obs-oracle, WP1 koopman removal, WP2 six+1 eval/deploy defects, WP3 dead knobs, WP5 delete envs/full_dof+envs/tdc, WP4 envs/_core → algorithms, WP6a _pathsetup, WP8 log_probs drop, WP10a lint, WP11 GRU per-env done hidden reset, segmented refusal, OU test deletion) and `marinelab` branch `cleanup/2026-09` (3 commits on 8cc84a5: EpisodeBuffer.log_probs drop, utils shim, shadowed duplicate test block)
- ground: **2 (volume)** — 14,884-line diff; the session read the conflict hunks, the CHANGELOG ledgers, and reproduced the branch's own gates itself; the per-commit semantic read went to two reviewers
- why native, not a vendor (omo obligation): codex quota locked until 14:35 (`turn.failed: usage limit`, measured 12:26); agy headless produced grep fragments only (see `2026-09-07-sweep.md`). Two native `oh-my-claudecode:code-reviewer` agents (opus), read-only on the container, reports written to files (channel truncates near 4 KB):
  - `rev-env-semantics` → `2026-09-07-cleanup-merge-review-R1.md` (WP0/1/3/5/4/6a/8 + marinelab): NEEDS-FIX 1 (WP3 also deleted the `@property` on `ConstraintEncoderRunner._should_log`; masked today by rsl_rl's own `learn` guard), plus: chain gate F3, plantdiff key-set F4, stale OU comment F5, all four removals proven inert on the SimToReal task with the p5 overrides, hunk-by-hunk resolution matching the session's, reverse-direction `doraemon_state.pt` hazard (post-merge state has no `buffer_log_probs`)
  - `rev-fixes-tests` → `2026-09-07-cleanup-merge-review-R2.md` (WP2/11/_REPO_ROOT/10a/segmented/tests): NEEDS-FIX 2 (D8 made one corrupt manifest abort every `find_runs` scan; segmented refusal collides with the p5 eval.py rewrite — resolved as R2 proposes), WP11 verified correct against the collector's `(envs, T)` layout, test ledger base→tip with every removal justified, `test_cli_emits_json` weakened to a skip, two tests import live marinelab by name
- what the session verified itself (not quoted from commit messages): R7 fixed-action GPU replay cleanup-tip vs baseline db1bcc6 = 10/10 arrays identical; test counts 497 → 510 with no net removal; merge-tree replay vs the exp oracle (d1fb67d, live marinelab f70f752) = 10/10 identical once marinelab was pinned to the merged worktree (first attempt differed from step 1 on cols 3–13/20–49/66–71 because PYTHONPATH pointed at the cleanup marinelab that predates candidate B `velocity_limit_sim` 2.40 — a test-setup confound, not a branch defect); `test_simtoreal_cfg.py` PASS 340 fields / exactly 7 moved; root pure-torch tests pass
- merge artifacts (isolated worktrees, live trees untouched): albc `/tmp/albc-merge` branch `merge/cleanup-into-exp` — 1e907a1 (merge, 9 hand-resolved hunks in config.py / albc_env.py / eval.py: `_MAIN_CONSTRAINT_TERMS` + "Probabilistic (4)"; keep every p5 Fz-disturbance method and call site, drop only `ou_*`; `_resample_commands` = p5 heading semantics minus the two identity scale factors; eval.py = p5 latched `_hold_yaw` + cleanup `hasattr` guards, switching-eval yaw `0.0` + cleanup's refusal comment) → 4f04a73 (review fixes: property restored, corrupt manifest skipped at the scan boundary with a test, stale docstring/comment removed, CHANGELOG lists all seven WP2 defects + merge-review corrections) → **b10d4df** (p5 test debt: `test_doraemon` fake cfg gains the two new DORAEMON dims, `test_eval_plots` fixture gains `yaw`; both failed identically on d1fb67d, control run in `/tmp/albc-exp`). Suite at b10d4df: 513 passed, 13 skipped. marinelab `/tmp/ml-merge` branch `merge/cleanup-into-main` — **3f5d4fa** (= f70f752 + cleanup), 63 tests pass.
- decision (session, operator delegated timing before sleeping 14:08): do not stop the running p5 teacher (cleanup is behavior-preserving on its path; ~10 h GPU at stake); land after `P5_PACK_DONE`, both repos together (mixed states raise `TypeError` at the first DORAEMON `record_episodes`, minutes into a launch — R1 §C3), then fast-forward `main`, then retarget `EXPECT_HEAD` for any later launch. Keeping the p5 student on the pre-WP11 code also keeps it one-variable against R3a; a WP11 student arm is a separate launch if the Thursday budget allows.
- second family (codex, ground 4, 14:49): LAND -- all 9 hunks REFUTED as defects, no deleted/renamed name survives in the three files; CANNOT-DECIDE items closed from the repo by the session (`_should_log` attribute reads at :166/:177/:184; `resolve_run`/`emit_run_manifest` read the manifest directly, paths.py:282/292/390). Report: `2026-09-07-cleanup-merge-codex.md`. eval.py path (not covered by the replay) smoke-tested as one exam cell on exp vs merge worktrees, GPU1, model_1700.pt.
- eval.py exam-cell smoke result (pair34 cell, 16 envs, seed 42, `--env-dr-anchor`, model_1700.pt, GPU1): the first pair (exp 15:01, merge 15:12) read the LIVE run dir, whose `doraemon_state.pt` the teacher rewrites at every save, so the two runs loaded different DORAEMON means (added_mass 1.0137 vs 1.0204) and soft/medium/hard differed while the none tier was bitwise identical (45 "diffs", all nan==nan). Rerun 15:23-15:43 against a frozen snapshot of the run dir (`/tmp/evalsmoke_frozen_run`: model_1700.pt + doraemon_state.pt + params/ + git/), PYTHONPATH pinned per tree and confirmed with `importlib.util.find_spec` (exp -> /workspace/marinelab f70f752, merge -> /tmp/ml-merge 3f5d4fa): **410/410 summary keys identical across none/soft/medium/hard** (`/tmp/evalsmoke2.sh`, outputs `/tmp/evalsmoke2_{exp,merge}/`). With this the merge tip b10d4df is verified on the training rollout (replay), the test suite, and the eval path. Side observation for the p5 report: the `--env-dr-anchor` banner prints `control_delay_steps=(0, 3)` while the run`s `params/env.yaml` records `(0, 13)` -- consistent with finding/396 (the anchor restores the dr_* arrays, not the launch overrides) and moot for the exam, whose delay cells are fixed via `--control-delay` (codex F3). Lesson: any A/B eval against a run that is still training must snapshot the run dir first; the checkpoint file is frozen but its sidecar state is not.

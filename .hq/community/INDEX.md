# INDEX — albc

> 311 posts · regenerated 2026-09-04 by `hq index`

## decision
- `decision/002` [subject: a5-budgetslack-rp-vel-settling-manipulability-budgets-x100-l] A5 budgetslack (rp_vel_settling + manipulability budgets x100): learner is anchor's twin, verdict CONTINGENT on seed floor — A5 budgetslack (rp_vel_settling + manipulability budgets x100): learner is anchor's twin, verdict CONTINGENT on seed floor
- `decision/003` [subject: a-checkpoint-ranking-established-at-none-can-dissolve-at-har] A checkpoint ranking established at none can dissolve at hard and ood: re-test finalists out of distribution before picking a deployment checkpoint — A checkpoint ranking established at none can dissolve at hard and ood: re-test finalists out of distribution before picking a deployment checkpoint
- `decision/004` [subject: a-cross-run-verdict-read-only-at-none-says-nothing-about-har] A cross-run verdict read only at none says nothing about hard: anchor asymmetry makes the hard exams incomparable, and re-scoring under one saturated anchor is 30 minutes with no retraining — A cross-run verdict read only at none says nothing about hard: anchor asymmetry makes the hard exams incomparable, and re-scoring under one saturated anchor is 30 minutes with no retraining
- `decision/006` [subject: a-one-variable-retrain-must-be-verified-against-the-baseline] A one-variable retrain must be verified against the BASELINE RUN's recorded config/env.yaml, not against the committed git diff -- a baseline launched dirty hides plant flags from every source-level check — A one-variable retrain must be verified against the BASELINE RUN's recorded config/env.yaml, not against the committed git diff -- a baseline launched dirty hides plant flags from every source-level check
- `decision/008` [subject: a-resolved-wiki-page-does-not-protect-a-launch-the-cudnn-pre] A resolved wiki page does not protect a launch: the cuDNN preamble was missing from the TCN launch script and cost 37 min at 18.9 s/iter — A resolved wiki page does not protect a launch: the cuDNN preamble was missing from the TCN launch script and cost 37 min at 18.9 s/iter
- `decision/009` [subject: a-smoke-run-that-never-reaches-the-failing-branch-is-not-ver] A smoke run that never reaches the failing branch is not verification: size it to the branch, or prove the fix by removing it — A smoke run that never reaches the failing branch is not verification: size it to the branch, or prove the fix by removing it
- `decision/012` [subject: action-bounding-is-justified-raw-gaussian-external-clamp-tan] Action bounding is justified (raw Gaussian + external clamp) -- tanh ruled out, 3 experiment leads remain — Action bounding is justified (raw Gaussian + external clamp) -- tanh ruled out, 3 experiment leads remain
- `decision/016` [subject: adding-a-dr-axis-is-half-a-change-dr-config-s-dr-tuple-field] Adding a DR axis is half a change: dr_config's _DR_TUPLE_FIELDS / _TRUE_NOMINAL_PHYSICS must both register it or the none level silently keeps the training band — Adding a DR axis is half a change: dr_config's _DR_TUPLE_FIELDS / _TRUE_NOMINAL_PHYSICS must both register it or the none level silently keeps the training band
- `decision/021` [subject: albc-stage-2-is-teacher-driven-off-policy-bc-with-mixed-late] ALBC stage 2 is teacher-driven off-policy BC with mixed latent+action labels (differs from RMA/HORA on both axes) — ALBC stage 2 is teacher-driven off-policy BC with mixed latent+action labels (differs from RMA/HORA on both axes)
- `decision/022` [subject: an-eval-schedule-too-sparse-to-resolve-the-curve-manufacture] An eval schedule too sparse to resolve the curve manufactures a false plateau and fires the stop rule on an artifact — An eval schedule too sparse to resolve the curve manufactures a false plateau and fires the stop rule on an artifact
- `decision/026` [subject: any-arm-vs-baseline-eval-in-this-repo-needs-doraemon-dr-from] Any arm-vs-baseline eval in this repo needs --doraemon-dr-from or the arms are judged on different test distributions — Any arm-vs-baseline eval in this repo needs --doraemon-dr-from or the arms are judged on different test distributions
- `decision/027` [subject: april-2026-entropy-collapse-campaign-machinery-bug-solved-co] April 2026 entropy-collapse campaign: machinery bug SOLVED, converged-sigma collapse NOT -- and min_std is the wrong lever — April 2026 entropy-collapse campaign: machinery bug SOLVED, converged-sigma collapse NOT -- and min_std is the wrong lever
- `decision/028` [subject: arm-a-measured-angle-joint1-constraint-recovers-not-diverges] Arm A (measured-angle joint1 constraint) RECOVERS, not diverges — the earlier divergence verdict was an early-stop artifact — Arm A (measured-angle joint1 constraint) RECOVERS, not diverges — the earlier divergence verdict was an early-stop artifact
- `decision/029` [subject: arm-b-is-a-dictionary-only-control-not-a-koopman-result-it-f] Arm B is a dictionary-only control, not a Koopman result: it fitted no operator, so citing its NULL as evidence against Koopman is an indefensible overclaim — Arm B is a dictionary-only control, not a Koopman result: it fitted no operator, so citing its NULL as evidence against Koopman is an indefensible overclaim
- `decision/030` [subject: arm-velocity-limit-sim-6-28-3-1-ripple-dead-constraint-trap-] Arm velocity_limit_sim 6.28->3.1 ripple: dead-constraint trap + delta_scale runaway (retrain item, not one-line) — Arm velocity_limit_sim 6.28->3.1 ripple: dead-constraint trap + delta_scale runaway (retrain item, not one-line)
- `decision/032` [subject: attitude-only-ablation-arms-registered-policy-obs-dim-sync-m] Attitude-only ablation arms registered; policy_obs_dim sync must cover every encoder-capable runner — Attitude-only ablation arms registered; policy_obs_dim sync must cover every encoder-capable runner
- `decision/039` [subject: bias-ema-observability-69-72d-improves-absolute-attitude-tra] bias_ema observability (69->72D) improves absolute attitude tracking + CV but does NOT shrink the hard-DR heavy-tail ratio -- the DC-bias tail is authority-limited (p7_tail e2) — bias_ema observability (69->72D) improves absolute attitude tracking + CV but does NOT shrink the hard-DR heavy-tail ratio -- the DC-bias tail is authority-limited (p7_tail e2)
- `decision/040` [subject: bias-ema-observability-p-b1-confirmed-at-the-fair-none-point] bias_ema observability (P-B1) confirmed at the fair 'none' point (-68% roll, -29% pitch) AND unexpectedly cleared the DORAEMON feasibility stall on unchanged lb=250 — bias_ema observability (P-B1) confirmed at the fair 'none' point (-68% roll, -29% pitch) AND unexpectedly cleared the DORAEMON feasibility stall on unchanged lb=250
- `decision/041` [subject: bias-reward-bias-ema-penalty-theory-review-conditionally-sou] bias reward (bias_ema_penalty) theory review: conditionally sound; hidden-state-dependent non-Markov reward is the key flaw (expose _bias_ema as obs), squared form does not fill dead-zone, penalty is 175x smaller than tracking loss — bias reward (bias_ema_penalty) theory review: conditionally sound; hidden-state-dependent non-Markov reward is the key flaw (expose _bias_ema as obs), squared form does not fill dead-zone, penalty is 175x smaller than tracking loss
- `decision/044` [subject: c3-s-dirty-tree-provenance-doubt-is-closed-a-dim-0-control-o] C3's dirty-tree provenance doubt is CLOSED: a dim=0 control on commit d81e2fd reproduces C3's iteration 0 bit-identically, so every student_distill_eint comparison against C3 stands — C3's dirty-tree provenance doubt is CLOSED: a dim=0 control on commit d81e2fd reproduces C3's iteration 0 bit-identically, so every student_distill_eint comparison against C3 stands
- `decision/046` [subject: calibrating-a-rotation-needs-two-points-and-a-named-referenc] Calibrating a rotation needs TWO points and a NAMED reference object -- the ALBC J1 zero moved four times in one day for want of both — Calibrating a rotation needs TWO points and a NAMED reference object -- the ALBC J1 zero moved four times in one day for want of both
- `decision/048` [subject: canonical-final-teacher-baseline-buoyfix-anchor-trpo-buoyanc] CANONICAL final teacher baseline = buoyfix anchor trpo_buoyanchor (biasema 72D-obs config on post-TAM + neutral-buoyancy plant, workstation seed 30); remaining work is small tweak-tests against it; B0c is the one open lever that can still change it — CANONICAL final teacher baseline = buoyfix anchor trpo_buoyanchor (biasema 72D-obs config on post-TAM + neutral-buoyancy plant, workstation seed 30); remaining work is small tweak-tests against it; B0c is the one open lever that can still change it
- `decision/049` [subject: closed-loop-latent-collapse-suspicion-legacy-student-measure] Closed-loop latent collapse suspicion: legacy student measured 11-17x worse in-loop, deployed student unverified — Closed-loop latent collapse suspicion: legacy student measured 11-17x worse in-loop, deployed student unverified
- `decision/051` [subject: constrained-albc-experiment-conventions] constrained-albc experiment conventions — constrained-albc experiment conventions
- `decision/055` [subject: constraint-threshold-budget-tuning-thresholds-split-into-har] Constraint threshold/budget tuning: thresholds split into hard physical rails (soft-inside-hard vs PhysX cap) vs soft shaping thresholds; budgets mostly no-op (9/10 slack); co-tune one at a time — Constraint threshold/budget tuning: thresholds split into hard physical rails (soft-inside-hard vs PhysX cap) vs soft shaping thresholds; budgets mostly no-op (9/10 slack); co-tune one at a time
- `decision/059` [subject: container-cudnn-is-cu13-against-cu128-torch-every-conv1d-fai] Container cuDNN is cu13 against cu128 torch: every conv1d fails, student distillation is 70x slower with the workaround — Container cuDNN is cu13 against cu128 torch: every conv1d fails, student distillation is 70x slower with the workaround
- `decision/060` [subject: coverage-lint-ok-is-a-floor-not-a-quality-gate-re-analysis-u] coverage lint ok is a floor not a quality gate; re-analysis uses OLD report as base, never shrinks — coverage lint ok is a floor not a quality gate; re-analysis uses OLD report as base, never shrinks
- `decision/061` [subject: cross-run-dr-comparability-eval-py-doraemon-dr-from-already-] Cross-run DR comparability: eval.py --doraemon-dr-from already provides a common test distribution; p7_tail knew and declined, judging a robustness campaign on nominal-only — Cross-run DR comparability: eval.py --doraemon-dr-from already provides a common test distribution; p7_tail knew and declined, judging a robustness campaign on nominal-only
- `decision/062` [subject: cross-run-reference-values-must-be-re-extracted-fresh-never-] cross-run reference values must be RE-EXTRACTED fresh, never carried forward (E4 stale-teacher-column gate) — cross-run reference values must be RE-EXTRACTED fresh, never carried forward (E4 stale-teacher-column gate)
- `decision/063` [subject: curriculum-recalibration-protocol-widening-the-dr-box-requir] Curriculum recalibration protocol: widening the DR box requires re-tuning budget (kl_ub x n_updates) AND performance_lb together -- not a single-variable probe — Curriculum recalibration protocol: widening the DR box requires re-tuning budget (kl_ub x n_updates) AND performance_lb together -- not a single-variable probe
- `decision/064` [subject: decision-do-not-adopt-performance-lb-200-on-the-adopted-bias] DECISION: do NOT adopt performance_lb=200 on the adopted bias_ema-ON config -- success 0.989 makes the feasibility constraint inert (the lb=68 failure class) — DECISION: do NOT adopt performance_lb=200 on the adopted bias_ema-ON config -- success 0.989 makes the feasibility constraint inert (the lb=68 failure class)
- `decision/069` [subject: doraemon-alpha-is-a-feasibility-floor-not-a-dr-expansion-lev] DORAEMON alpha is a feasibility FLOOR, not a DR-expansion lever (E5 dr-harder) — DORAEMON alpha is a feasibility FLOOR, not a DR-expansion lever (E5 dr-harder)
- `decision/076` [subject: doraemon-mean-and-doraemon-std-tb-tags-are-raw-physical-unit] DORAEMON/mean and DORAEMON/std TB tags are RAW PHYSICAL UNITS, not normalized xi -- Beta(a,b) inversion from them is valid for only 3 of the 20 params — DORAEMON/mean and DORAEMON/std TB tags are RAW PHYSICAL UNITS, not normalized xi -- Beta(a,b) inversion from them is valid for only 3 of the 20 params
- `decision/078` [subject: doraemon-reactivity-makes-every-single-variable-probe-two-va] DORAEMON reactivity makes every single-variable probe two-variable: the curriculum is the uncontrolled second variable (p7_tail e1/e3/e4) — DORAEMON reactivity makes every single-variable probe two-variable: the curriculum is the uncontrolled second variable (p7_tail e1/e3/e4)
- `decision/081` [subject: dr-harder-campaign-synthesis-speed-kills-attitude-center-shi] DR-harder campaign synthesis: speed kills attitude, center-shift overfits — DR-harder campaign synthesis: speed kills attitude, center-shift overfits
- `decision/082` [subject: dr-harder-ood-verdict-e2-best-survives-ood-the-harder-curric] DR-harder OOD verdict: E2-best survives OOD; the harder curriculum (E1) generalizes WORST — DR-harder OOD verdict: E2-best survives OOD; the harder curriculum (E1) generalizes WORST
- `decision/085` [subject: e3-extend10k-regressed-the-policy-training-budget-is-not-the] e3 extend10k REGRESSED the policy — training budget is NOT the p7_tail tail lever; keep 5000 iters — e3 extend10k REGRESSED the policy — training budget is NOT the p7_tail tail lever; keep 5000 iters
- `decision/086` [subject: e3-s-5000-iter-budget-verdict-is-scope-limited-not-a-cap-max] e3's '5000-iter budget' verdict is scope-limited, NOT a cap: max_iterations is a DR-EXPANSION knob (step_interval clock) and the real ceiling is the Beta a=b=1 config bound, not compute — e3's '5000-iter budget' verdict is scope-limited, NOT a cap: max_iterations is a DR-EXPANSION knob (step_interval clock) and the real ceiling is the Beta a=b=1 config bound, not compute
- `decision/089` [subject: ee-leak-0-k-anchor-0-does-not-blow-up-joint1-settle-pade-ik-] ee_leak=0 + k_anchor=0 does NOT blow up joint1 settle (Pade/IK clamp bounds it); settle still bimodal — ee_leak=0 + k_anchor=0 does NOT blow up joint1 settle (Pade/IK clamp bounds it); settle still bimodal
- `decision/094` [subject: engine-gap-analyze-training-py-emits-no-reward-8-term-decomp] engine-gap: analyze_training.py emits no reward 8-term decomposition scalars — engine-gap: analyze_training.py emits no reward 8-term decomposition scalars
- `decision/095` [subject: engine-gap-analyze-training-py-needs-the-logs-run-dir-not-th] engine-gap: analyze_training.py needs the logs/ run dir, not the experiments/ run dir (events under train symlink) — engine-gap: analyze_training.py needs the logs/ run dir, not the experiments/ run dir (events under train symlink)
- `decision/096` [subject: engine-gap-analyze-training-py-only-runs-under-isaac-sim-pyt] engine-gap: analyze_training.py only runs under /isaac-sim/python.sh (system numpy 2.5.1 breaks scipy); --deep backends silently absent — engine-gap: analyze_training.py only runs under /isaac-sim/python.sh (system numpy 2.5.1 breaks scipy); --deep backends silently absent
- `decision/097` [subject: engine-gap-decision-floors-are-field-keyed-while-units-are-a] engine-gap: decision floors are field-keyed while units are axis-keyed, so steady-state error is unadjudicated on 4 of 7 axes — engine-gap: decision floors are field-keyed while units are axis-keyed, so steady-state error is unadjudicated on 4 of 7 axes
- `decision/098` [subject: engine-gap-doraemon-per-parameter-curriculum-expansion-is-in] engine-gap: DORAEMON per-parameter curriculum expansion is invisible in the text diagnosis (TIER 2 prints only success/ess/mode) — engine-gap: DORAEMON per-parameter curriculum expansion is invisible in the text diagnosis (TIER 2 prints only success/ess/mode)
- `decision/099` [subject: engine-gap-eval-adapter-covers-static-segmented-periodic-sti] engine-gap: eval adapter covers static + segmented; periodic still unsupported — engine-gap: eval adapter covers static + segmented; periodic still unsupported
- `decision/100` [subject: engine-gap-eval-adapter-heavy-tail-lacks-median-based-tail-r] engine-gap: eval_adapter heavy-tail lacks median-based tail ratio + top-k concentration — engine-gap: eval_adapter heavy-tail lacks median-based tail ratio + top-k concentration
- `decision/101` [subject: engine-gap-eval-npz-saves-no-raw-obs-std-privileged-blocks-e] engine-gap: eval npz saves no raw obs/std/privileged — blocks exact per-env std reconstruction — engine-gap: eval npz saves no raw obs/std/privileged — blocks exact per-env std reconstruction
- `decision/102` [subject: engine-gap-flat-target-eval-records-joint1-trajectory-but-re] engine-gap: flat-target eval records joint1 trajectory but renders no drift plot — engine-gap: flat-target eval records joint1 trajectory but renders no drift plot
- `decision/103` [subject: engine-gap-heavy-tail-json-pct-peak-gt-thresh-exceeds-100-at] engine-gap: heavy_tail.json pct_peak_gt_thresh exceeds 100% at ood level (denominator bug) — engine-gap: heavy_tail.json pct_peak_gt_thresh exceeds 100% at ood level (denominator bug)
- `decision/104` [subject: engine-gap-m1-critic-side-encoder-gradient-is-not-logged-pol] engine-gap: M1 critic-side encoder gradient is not logged (Policy/encoder_grad_norm is actor-path only) — engine-gap: M1 critic-side encoder gradient is not logged (Policy/encoder_grad_norm is actor-path only)
- `decision/105` [subject: engine-gap-omx-cli-gaps-found-re-analyzing-teacher-2026-06-0] engine-gap: omx CLI gaps found re-analyzing teacher (2026-06-06 round 2) — engine-gap: omx CLI gaps found re-analyzing teacher (2026-06-06 round 2)
- `decision/106` [subject: engine-gap-omx-plot-is-unusable-on-this-workspace-tb-blocked] engine-gap: omx plot is unusable on this workspace -- TB blocked by system-python tensorboard, eval npz blocked by 1-D-only series — engine-gap: omx plot is unusable on this workspace -- TB blocked by system-python tensorboard, eval npz blocked by 1-D-only series
- `decision/107` [subject: engine-gap-omx-reduce-tb-final-unusable-here-cli-on-system-p] engine-gap: omx reduce tb-final unusable here (CLI on system python without tensorboard) + no paired-condition comparison in the analysis package — engine-gap: omx reduce tb-final unusable here (CLI on system python without tensorboard) + no paired-condition comparison in the analysis package
- `decision/108` [subject: engine-gap-omx-wiki-capture-dedupes-by-raw-path-string-and-a] engine-gap: omx wiki capture dedupes by raw path string and appends without an equality check, so one report can be captured twice byte-identically — engine-gap: omx wiki capture dedupes by raw path string and appends without an equality check, so one report can be captured twice byte-identically
- `decision/109` [subject: engine-gap-qualityscore-is-computed-on-the-incoming-append-o] engine-gap: qualityScore is computed on the incoming append only, so a one-line housekeeping update permanently downgrades a rich page — engine-gap: qualityScore is computed on the incoming append only, so a one-line housekeeping update permanently downgrades a rich page
- `decision/110` [subject: engine-gap-the-analysis-engine-and-omx-reduce-are-both-unusa] engine-gap: the analysis engine and omx reduce are both unusable on student distillation runs — engine-gap: the analysis engine and omx reduce are both unusable on student distillation runs
- `decision/111` [subject: engine-gap-the-latent-fidelity-ratio-has-no-owning-module-an] engine-gap: the latent fidelity ratio has no owning module, and it must be reported against per-dim R2 because their divergence is the diagnostic — engine-gap: the latent fidelity ratio has no owning module, and it must be reported against per-dim R2 because their divergence is the diagnostic
- `decision/114` [subject: eval-analysis-core-does-stats-adapter-does-heavy-tail] eval analysis: core does stats, adapter does heavy-tail — eval analysis: core does stats, adapter does heavy-tail
- `decision/115` [subject: eval-command-box-was-half-the-trained-envelope-from-2026-04-] Eval command box was HALF the trained envelope from 2026-04-06 to 2026-07-15 -- every pre-posttam attitude number is optimistic — Eval command box was HALF the trained envelope from 2026-04-06 to 2026-07-15 -- every pre-posttam attitude number is optimistic
- `decision/117` [subject: eval-metric-units-and-decision-floors-os-env-mean-is-percent] Eval metric units and decision floors: os_env_mean is percent-of-step (roll steps 30 deg), ss_error is degrees; paired screening floors and the machine-isolation rule — Eval metric units and decision floors: os_env_mean is percent-of-step (roll steps 30 deg), ss_error is degrees; paired screening floors and the machine-isolation rule
- `decision/118` [subject: eval-npz-fault-block-contract-fault-injection-flag-fault-key] eval npz fault block contract: fault_injection flag, fault_ keys absent-by-design on healthy evals — eval npz fault block contract: fault_injection flag, fault_ keys absent-by-design on healthy evals
- `decision/119` [subject: eval-py-rebuilds-env-cfg-from-hydra-defaults-so-obs-widening] eval.py rebuilds env cfg from Hydra defaults, so obs-widening flags must be re-passed at eval time — eval.py rebuilds env cfg from Hydra defaults, so obs-widening flags must be re-passed at eval time
- `decision/120` [subject: eval-py-static-doraemon-dr-grades-each-run-on-its-own-learne] eval.py static --doraemon-dr grades each run on its OWN learned DR — cross-run hard/ood is non-comparable, only none is fair — eval.py static --doraemon-dr grades each run on its OWN learned DR — cross-run hard/ood is non-comparable, only none is fair
- `decision/123` [subject: eval-static-cross-run-pairing-requires-per-level-reseed] eval static cross-run pairing requires per-level reseed — eval static cross-run pairing requires per-level reseed
- `decision/125` [subject: exp-analyze-must-run-analyze-training-py-for-training-side-d] exp-analyze MUST run analyze_training.py for training-side diagnosis — exp-analyze MUST run analyze_training.py for training-side diagnosis
- `decision/126` [subject: experiment-idea-feed-o-t-into-the-encoder-alongside-p-t-stat] experiment idea: feed o_t into the encoder alongside p_t (state-conditioned z, RMA-style) instead of p_t-only encoder input — experiment idea: feed o_t into the encoder alongside p_t (state-conditioned z, RMA-style) instead of p_t-only encoder input
- `decision/127` [subject: experiment-idea-latency-transport-delay-dr-sensor-obs-contro] experiment idea: latency/transport-delay DR (sensor-obs + control-action lag) -- infra exists (isaaclab DelayBuffer) but unused; DelayedPD failed before — experiment idea: latency/transport-delay DR (sensor-obs + control-action lag) -- infra exists (isaaclab DelayBuffer) but unused; DelayedPD failed before
- `decision/128` [subject: experiment-launch-checklist-run-id-wandb-latest-alias-naming] experiment launch checklist: run_id / wandb / latest-alias naming — experiment launch checklist: run_id / wandb / latest-alias naming
- `decision/129` [subject: experiment-output-directory-standard-logs-vs-experiments-ind] experiment output directory standard (logs vs experiments index tree) — experiment output directory standard (logs vs experiments index tree)
- `decision/130` [subject: experiment-result-recording-location-experiments-tree-is-sso] experiment result recording location (experiments tree is SSOT, not docs/results) — experiment result recording location (experiments tree is SSOT, not docs/results)
- `decision/132` [subject: extend8k-8000-iter-confirms-e3-extending-past-5000-iters-is-] extend8k (8000-iter) confirms e3: extending past 5000 iters is net-negative on the FIXED-DR exam, an axis trade, and confounded by DR-width expansion — extend8k (8000-iter) confirms e3: extending past 5000 iters is net-negative on the FIXED-DR exam, an axis trade, and confounded by DR-width expansion
- `decision/135` [subject: failure-dr-join-now-covers-fault-channels-not-just-dr-per-en] failure_dr join now covers fault_ channels, not just dr_ (per-env failure<->FAULT correlation enabled) — failure_dr join now covers fault_ channels, not just dr_ (per-env failure<->FAULT correlation enabled)
- `decision/140` [subject: ftc-fault-dr-a-b-result-2026-07-27-fault-dr-adopted-5-12x-le] FTC fault-DR A/B result 2026-07-27: fault-DR adopted (5-12x less m4-dead degradation, zero terminations, tail removal); privileged fault obs NOT adopted (H2); fault_severity curriculum ended under-expanded at 8-10% of range — FTC fault-DR A/B result 2026-07-27: fault-DR adopted (5-12x less m4-dead degradation, zero terminations, tail removal); privileged fault obs NOT adopted (H2); fault_severity curriculum ended under-expanded at 8-10% of range
- `decision/143` [subject: gpu-memory-4096-env-albc-training-needs-11-3-gb-rtx-4060-8gb] GPU memory: 4096-env ALBC training needs ~11.3 GB — RTX 4060 8GB cannot co-run a second experiment — GPU memory: 4096-env ALBC training needs ~11.3 GB — RTX 4060 8GB cannot co-run a second experiment
- `decision/144` [subject: group-folder-name-equals-wandb-project-name-equals-experimen] Group folder name equals wandb project name equals experiment purpose (unified naming, 2026-07-14 revision) — Group folder name equals wandb project name equals experiment purpose (unified naming, 2026-07-14 revision)
- `decision/145` [subject: gru-memory-and-corrected-dagger-mixing-compound-c3-is-the-ca] GRU memory and corrected DAgger mixing COMPOUND: C3 is the campaign's best latent tracker at every level and the first student to beat the teacher by a decision-grade margin — GRU memory and corrected DAgger mixing COMPOUND: C3 is the campaign's best latent tracker at every level and the first student to beat the teacher by a decision-grade margin
- `decision/149` [subject: how-to-read-omx-wiki-lint-on-this-corpus-oversized-means-acc] How to read omx wiki lint on this corpus: oversized means accumulated updates, and contradiction-candidate is a tag-collision heuristic that never reads a claim — How to read omx wiki lint on this corpus: oversized means accumulated updates, and contradiction-candidate is a tag-collision heuristic that never reads a claim
- `decision/151` [subject: hydrorc-016d1b1-recentered-nine-hull-hydro-numbers-onto-a-br] HydroRC 016d1b1 recentered nine hull hydro numbers onto a broken engine approximation, and the damage concentrates on yaw -- the axis where the 2026-07-28 paired gate failed; retire the commit rather than rebuild it minus one line — HydroRC 016d1b1 recentered nine hull hydro numbers onto a broken engine approximation, and the damage concentrates on yaw -- the axis where the 2026-07-28 paired gate failed; retire the commit rather than rebuild it minus one line
- `decision/153` [subject: hydrorc-recenter-gate-result-2026-07-28-isaac-paired-gate-fa] HydroRC recenter gate result 2026-07-28: Isaac paired gate FAIL (roll n_gt20 0->18.7 envs, yaw ss +18.8%) -- transient-tail regression at all DR levels, hard-corner collapse, 7-17x fault-robustness loss; recenter not adopted, Stonefish readout not entered — HydroRC recenter gate result 2026-07-28: Isaac paired gate FAIL (roll n_gt20 0->18.7 envs, yaw ss +18.8%) -- transient-tail regression at all DR levels, hard-corner collapse, 7-17x fault-robustness loss; recenter not adopted, Stonefish readout not entered
- `decision/155` [subject: incident-post-mortem-teacher-baseline-opt-e1-e4-trained-on-a] INCIDENT post-mortem: teacher_baseline_opt + e1-e4 trained on a TAM the wiki knew was wrong; measured TAM/IMU/TAM-DR dropped at P4 batch, other batch items applied — INCIDENT post-mortem: teacher_baseline_opt + e1-e4 trained on a TAM the wiki knew was wrong; measured TAM/IMU/TAM-DR dropped at P4 batch, other batch items applied
- `decision/156` [subject: joint1-anti-drift-constrain-the-command-cumulative-arm-b-not] joint1 anti-drift: constrain the COMMAND (cumulative, Arm B) not the MEASUREMENT (wrap, Arm A) to preserve attitude — joint1 anti-drift: constrain the COMMAND (cumulative, Arm B) not the MEASUREMENT (wrap, Arm A) to preserve attitude
- `decision/161` [subject: joint1-stage-1-gate-go-drift-is-real-on-unlimited-physics-no] joint1 Stage-1 gate GO: drift is real on unlimited physics, not the +-360deg wall artifact — joint1 Stage-1 gate GO: drift is real on unlimited physics, not the +-360deg wall artifact
- `decision/167` [subject: kl-ub-up-and-per-difficulty-learning-are-antagonistic-the-dr] kl_ub up and per-difficulty learning are antagonistic; the dr_harder verdict is budget-conditional — kl_ub up and per-difficulty learning are antagonistic; the dr_harder verdict is budget-conditional
- `decision/168` [subject: koopman-line-reopened-2026-08-05-the-2026-08-04-closure-was-] Koopman line REOPENED 2026-08-05: the 2026-08-04 closure was a budget decision under a control-performance goal, and no training arm ever fitted a Koopman operator — Koopman line REOPENED 2026-08-05: the 2026-08-04 closure was a budget decision under a control-performance goal, and no training arm ever fitted a Koopman operator
- `decision/169` [subject: koopman-phase-1-arm-b-null-marine-feature-lifting-buys-no-co] Koopman Phase 1 arm B NULL: marine-feature lifting buys no control quality and costs transient overshoot — Koopman Phase 1 arm B NULL: marine-feature lifting buys no control quality and costs transient overshoot
- `decision/171` [subject: lambda-latent-is-bracketed-and-closed-no-decision-grade-cont] Lambda_latent is bracketed and CLOSED: no decision-grade control effect in [0,4] and lambda=1 is a measured local optimum — Lambda_latent is bracketed and CLOSED: no decision-grade control effect in [0,4] and lambda=1 is a measured local optimum
- `decision/177` [subject: n-gt20-and-os-env-are-overshoot-percent-of-step-magnitude-no] n_gt20 and os_env_* are OVERSHOOT PERCENT of step magnitude, NOT degrees -- several posttam reports mislabel it as 'peak>20deg' — n_gt20 and os_env_* are OVERSHOOT PERCENT of step magnitude, NOT degrees -- several posttam reports mislabel it as 'peak>20deg'
- `decision/179` [subject: next-experiment-workflow-pick-a-baseline-train-once-then-re-] Next experiment workflow: pick a baseline, train once, then re-tune reward weights and retrain (two-phase) — Next experiment workflow: pick a baseline, train once, then re-tune reward weights and retrain (two-phase)
- `decision/180` [subject: next-from-scratch-retrain-manifest-what-rides-on-the-post-ta] Next from-scratch retrain manifest: what rides on the post-TAM baseline retrain (sim fixes + learning-dynamics experiments) — Next from-scratch retrain manifest: what rides on the post-TAM baseline retrain (sim fixes + learning-dynamics experiments)
- `decision/184` [subject: obs4-phase-d-result-2026-08-04-folding-the-4-deployable-chan] obs4 Phase D result 2026-08-04: folding the 4 deployable channels into policy_obs (72 -> 76) PASSES H1 with margin; it buys hard-DR tail and spread with a REAL pitch steady-state regression at soft/medium — obs4 Phase D result 2026-08-04: folding the 4 deployable channels into policy_obs (72 -> 76) PASSES H1 with margin; it buys hard-DR tail and spread with a REAL pitch steady-state regression at soft/medium
- `decision/185` [subject: obs4-student-extra-observation-interface-4-deployable-channe] obs4 student extra-observation interface: 4 deployable channels ride the observation dict through ONE shared student_input, zero-order held at the real 25 Hz bus rate; implemented and pushed 2026-08-03, not yet run — obs4 student extra-observation interface: 4 deployable channels ride the observation dict through ONE shared student_input, zero-order held at the real 25 Hz bus rate; implemented and pushed 2026-08-03, not yet run
- `decision/186` [subject: obs76-gen-2-student-reproduces-its-teacher-s-mean-tracking-a] obs76 gen-2 student reproduces its teacher's mean tracking at every DR level but triples roll dispersion at hard; in-loop latent R2 stays negative, so the observability intervention did not close covariate shift — obs76 gen-2 student reproduces its teacher's mean tracking at every DR level but triples roll dispersion at hard; in-loop latent R2 stays negative, so the observability intervention did not close covariate shift
- `decision/189` [subject: omx-wiki-has-no-delete-subcommand-by-design-removal-is-gc-ap] omx wiki has no delete subcommand by design: removal is gc-apply, duplicates come from add-with-new-title — omx wiki has no delete subcommand by design: removal is gc-apply, duplicates come from add-with-new-title
- `decision/190` [subject: omx-wiki-page-grammar-for-this-workspace-title-category-body] omx wiki page grammar for this workspace: title, category, body sections, and what a merge can never change — omx wiki page grammar for this workspace: title, category, body sections, and what a merge can never change
- `decision/191` [subject: omx-wiki-query-is-not-read-only-it-appends-to-registry-log-m] omx wiki query is not read-only: it appends to registry/log.md, so a pure lookup leaves the tree dirty — omx wiki query is not read-only: it appends to registry/log.md, so a pure lookup leaves the tree dirty
- `decision/192` [subject: on-policy-dagger-correction-for-the-buoyfix-student] On-policy DAgger correction for the buoyfix student — On-policy DAgger correction for the buoyfix student
- `decision/197` [subject: open-actionable-ledger-read-before-any-sim-plant-code-change] OPEN-ACTIONABLE LEDGER: read before any sim-plant code change or baseline launch (TAM/IMU HARD-gate + experiment backlog) — OPEN-ACTIONABLE LEDGER: read before any sim-plant code change or baseline launch (TAM/IMU HARD-gate + experiment backlog)
- `decision/200` [subject: p1-cross-sim-arm-swing-the-4-7-9-3x-joint-torque-gap-is-with] P1 cross-sim arm swing: the 4.7-9.3x joint-torque gap is WITHDRAWN as a servo discretisation artifact (corrected to 0.67x), and the arm-reaction channel is eliminated as a yaw-failure candidate in both sims — P1 cross-sim arm swing: the 4.7-9.3x joint-torque gap is WITHDRAWN as a servo discretisation artifact (corrected to 0.67x), and the arm-reaction channel is eliminated as a yaw-failure candidate in both sims
- `decision/201` [subject: p-a8-perflb200-more-iters-5000-8000-closed-the-deployment-oo] P-A8 (perflb200 more-iters 5000->8000) closed the deployment-OOD DR gap exactly as predicted, at a quantified nominal-tracking cost offset by heavy-tail reduction — P-A8 (perflb200 more-iters 5000->8000) closed the deployment-OOD DR gap exactly as predicted, at a quantified nominal-tracking cost offset by heavy-tail reduction
- `decision/202` [subject: p-b1-shared-exam-on-reference-dr-hard-roll-floor-was-exam-ar] P-B1 shared-exam on reference DR: hard-roll floor was exam artifact, transient peak trade survives (H2/e2) — P-B1 shared-exam on reference DR: hard-roll floor was exam artifact, transient peak trade survives (H2/e2)
- `decision/205` [subject: perflb200-final-dr-anatomy-17-bulk-params-at-config-ceiling-] perflb200 final DR anatomy: 17 bulk params at config ceiling (uniform), 3 deployment-relevant params (ocean_current/obs_noise/payload_cog) are TIME-limited not feasibility-limited — perflb200 final DR anatomy: 17 bulk params at config ceiling (uniform), 3 deployment-relevant params (ocean_current/obs_noise/payload_cog) are TIME-limited not feasibility-limited
- `decision/206` [subject: performance-lb-doraemon-gate-is-causally-independent-of-the-] performance_lb (DORAEMON gate) is causally independent of the actor exploration collapse — performance_lb (DORAEMON gate) is causally independent of the actor exploration collapse
- `decision/208` [subject: plan-consolidation-2026-07-23-canonical-ids-z-a-b-c-one-camp] Plan consolidation 2026-07-23: canonical ids Z/A/B/C + one-campaign-per-group; master doc docs/reference/teacher-campaign-plan.md — Plan consolidation 2026-07-23: canonical ids Z/A/B/C + one-campaign-per-group; master doc docs/reference/teacher-campaign-plan.md
- `decision/209` [subject: plant-change-batch-v2-four-isaac-plant-corrections-are-now-p] Plant-change batch v2: four Isaac plant corrections are now pending and each alone forces a teacher retrain, so they are batched behind one sizing gate instead of decided individually — Plant-change batch v2: four Isaac plant corrections are now pending and each alone forces a teacher retrain, so they are batched behind one sizing gate instead of decided individually
- `decision/210` [subject: plant-fix-needs-apply-before-retrain-main-hull-volume-0-009-] PLANT FIX (needs-apply-before-retrain): main hull volume 0.009 -> 0.00790 recenters sim net buoyancy +10.25 N -> neutral, matching 2026-07-06 onboard measurement — PLANT FIX (needs-apply-before-retrain): main hull volume 0.009 -> 0.00790 recenters sim net buoyancy +10.25 N -> neutral, matching 2026-07-06 onboard measurement
- `decision/216` [subject: proving-a-run-on-a-divergent-branch-is-still-on-the-referenc] Proving a run on a divergent branch is still on the reference plant — Proving a run on a divergent branch is still on the reference plant
- `decision/219` [subject: real-robot-has-2-faulted-thrusters-user-judges-buoyancy-rest] Real robot has 2 faulted thrusters; user judges buoyancy restoring dominates so baseline proceeds without FTC; thruster is in the actor output partly for gradient-vanishing avoidance (FTC under separate investigation) — Real robot has 2 faulted thrusters; user judges buoyancy restoring dominates so baseline proceeds without FTC; thruster is in the actor output partly for gradient-vanishing avoidance (FTC under separate investigation)
- `decision/227` [subject: reward-md-6-error-buffers-reward-call-site-dt-scaling-theory] reward.md §6 (error buffers / reward call site / dt-scaling) theory review: conditionally sound; the one real exposure is B4 (reward*dt with FIXED gamma = Tallec's dt-invariance pathology, horizon dt/(1-gamma)=2.0s) but LATENT since no run changes step_dt; B5 'contradiction' refuted (actor scale-invariant vs DORAEMON performance_lb raw-return gate = consumer distinction) — reward.md §6 (error buffers / reward call site / dt-scaling) theory review: conditionally sound; the one real exposure is B4 (reward*dt with FIXED gamma = Tallec's dt-invariance pathology, horizon dt/(1-gamma)=2.0s) but LATENT since no run changes step_dt; B5 'contradiction' refuted (actor scale-invariant vs DORAEMON performance_lb raw-return gate = consumer distinction)
- `decision/230` [subject: reward-sigma-integral-obs-gate-coupling-reward-md-7-theory-r] reward-sigma / integral-obs-gate coupling (reward.md 7) theory review: conditionally sound; shared-sigma ALIASING is the defect (decouple gate threshold), gate is a settling-band accumulator not anti-windup, clamp is dead code in gated mode, Hwangbo-2017 citation is wrong (use Yu&Lee 2023) — reward-sigma / integral-obs-gate coupling (reward.md 7) theory review: conditionally sound; shared-sigma ALIASING is the defect (decouple gate threshold), gate is a settling-band accumulator not anti-windup, clamp is dead code in gated mode, Hwangbo-2017 citation is wrong (use Yu&Lee 2023)
- `decision/234` [subject: safety-rail-constraints-fire-hard-at-init-go-quiet-and-re-fi] Safety-rail constraints fire hard at init, go quiet, and RE-FIRE as DORAEMON widens DR -- 0% at the final window is working insurance, not dead weight; thruster_util is the one trending into binding (93.2% at extend8k) — Safety-rail constraints fire hard at init, go quiet, and RE-FIRE as DORAEMON widens DR -- 0% at the final window is working insurance, not dead weight; thruster_util is the one trending into binding (93.2% at extend8k)
- `decision/236` [subject: seed-noise-floor-on-none-level-attitude-is-huge-75-peak-to-p] Seed-noise floor on none-level attitude is HUGE (~75% peak-to-peak on roll ss_error): the +/-5% adoption band is undecidable at n=1 — Seed-noise floor on none-level attitude is HUGE (~75% peak-to-peak on roll ss_error): the +/-5% adoption band is undecidable at n=1
- `decision/238` [subject: severity-init-head-start-converts-to-curriculum-2-50x-but-ma] Severity-init head start converts to curriculum (2.50x) but makes m4-dead fault rejection 2.9-5.5x WORSE -- raising trained fault severity is not a route to fault tolerance on this plant — Severity-init head start converts to curriculum (2.50x) but makes m4-dead fault rejection 2.9-5.5x WORSE -- raising trained fault severity is not a route to fault tolerance on this plant
- `decision/241` [subject: state-dependent-std-robustness-vs-nominal-trade-off-not-diff] state_dependent_std: robustness-vs-nominal trade-off, NOT difficulty-adaptive (Phase-2 falsification) — state_dependent_std: robustness-vs-nominal trade-off, NOT difficulty-adaptive (Phase-2 falsification)
- `decision/243` [subject: step-interval-250-400-probe-separate-dr-width-from-optimisat] step_interval 250->400 probe: separate DR-WIDTH from OPTIMISATION-STEPS as the cause of extend8k's nominal roll transient regression (pending approval, not launched) — step_interval 250->400 probe: separate DR-WIDTH from OPTIMISATION-STEPS as the cause of extend8k's nominal roll transient regression (pending approval, not launched)
- `decision/246` [subject: stonefish-role-narrowed-to-integration-smoke-bench-ratified-] Stonefish role narrowed to integration smoke bench (ratified 2026-08-03): no more absolute-performance or coefficient verdicts; real-robot anchors take priority; retrain principle = widen DR by measured uncertainty, never move coefficients to a simulator's value — Stonefish role narrowed to integration smoke bench (ratified 2026-08-03): no more absolute-performance or coefficient verdicts; real-robot anchors take priority; retrain principle = widen DR by measured uncertainty, never move coefficients to a simulator's value
- `decision/247` [subject: stonefish-rotational-drag-is-a-distributed-integral-with-no-] Stonefish rotational drag is a distributed integral with no separate rotational term, so hull yaw pressure drag is exactly zero by construction -- the 45.5x yaw damping gap is an artifact and roll/pitch is corrupted by a force-derived torque correction — Stonefish rotational drag is a distributed integral with no separate rotational term, so hull yaw pressure drag is exactly zero by construction -- the 45.5x yaw damping gap is an artifact and roll/pitch is corrupted by a force-derived torque correction
- `decision/252` [subject: student-eval-the-none-column-is-the-out-of-distribution-end-] Student eval: the none column is the OUT-OF-DISTRIBUTION end, not the easy end — Student eval: the none column is the OUT-OF-DISTRIBUTION end, not the easy end
- `decision/253` [subject: tam-columns-must-match-robot-firmware-esc-channel-order-reor] TAM columns must match robot firmware ESC channel order (reorder + retrain, not a mixer permutation) — TAM columns must match robot firmware ESC channel order (reorder + retrain, not a mixer permutation)
- `decision/254` [subject: tam-plant-correctness-fix-collapses-the-void-hard-dr-roll-he] TAM plant-correctness fix collapses the void hard-DR roll heavy-tail (into a raised floor) — TAM plant-correctness fix collapses the void hard-DR roll heavy-tail (into a raised floor)
- `decision/263` [subject: the-c3-recipe-does-not-transfer-across-teachers-on-a-same-wi] The C3 recipe does not transfer across teachers: on a same-width dgx16k teacher it loses at every DR level and reverses its hard win — The C3 recipe does not transfer across teachers: on a same-width dgx16k teacher it loses at every DR level and reverses its hard win
- `decision/267` [subject: the-doraemon-success-rate-peak-is-set-by-performance-lb-not-] The DORAEMON success_rate PEAK is set by performance_lb, not policy quality: lb=250 peaks 0.63-0.67 and ends CONTRACTED; lb=200 peaks 0.97 and reaches the config ceiling — The DORAEMON success_rate PEAK is set by performance_lb, not policy quality: lb=250 peaks 0.63-0.67 and ends CONTRACTED; lb=200 peaks 0.97 and reaches the config ceiling
- `decision/288` [subject: train-py-run-group-creates-the-group-layer-no-more-manual-po] train.py --run_group creates the <group> layer (no more manual post-move) — train.py --run_group creates the <group> layer (no more manual post-move)
- `decision/294` [subject: utracker-diffusion-policy-is-rejected-for-albc-on-two-indepe] UTracker diffusion policy is rejected for ALBC on two independent grounds, and its repo disagrees with its own paper on all six axes (do not use it as a comparison axis) — UTracker diffusion policy is rejected for ALBC on two independent grounds, and its repo disagrees with its own paper on all six axes (do not use it as a comparison axis)
- `decision/298` [subject: x1-tail-split-restores-the-gen-2-latent-collapse-but-moves-n] X1 tail-split restores the gen-2 latent collapse but moves NO control metric: latent reconstruction and closed-loop dispersion are decoupled — X1 tail-split restores the gen-2 latent collapse but moves NO control metric: latent reconstruction and closed-loop dispersion are decoupled
- `decision/299` [subject: xy-offset-dr-is-load-bearing-for-pitch-not-free-ndims-diluti] xy-offset DR is load-bearing for pitch, not free NDIMS dilution (e4 xyprune) — xy-offset DR is load-bearing for pitch, not free NDIMS dilution (e4 xyprune)
- `decision/300` [subject: yaw-command-is-rate-not-angle-inherited-design-defensible-on] yaw command is rate not angle: inherited design, defensible only if heading is a free DOF (yaw-angle A/B idea) — yaw command is rate not angle: inherited design, defensible only if heading is a free DOF (yaw-angle A/B idea)
- `decision/301` [subject: retrain-simtoreal-2026-09] Program retrain-simtoreal-2026-09 opened: teacher retrain on the real actuator set (m3 dead, m4 excluded) + control_delay (0,1) — pending user approval, 10 decisions listed — 2026-09-02: after vault finding/137 showed the incumbent learned pitch from the vertical thrusters (14.5 Nm) and never saw the real m3+m4-dead plant (2-dead exposure 1/30,000 per episode), a retrain program was opened at .hq/community/programs/retrain-simtoreal-2026-09/PLAN.md. Config delta vs incumbent is exactly two knobs (fault.thruster_fixed_health=[1,1,1,0,0,1]; control_delay_steps=(0,1)), 10000 iter, >=2 seeds, 4096 envs, everything else byte-identical; no new constraint or shaping. Phase 0 desk gates (G0-A fixed-health eval of the incumbent, G0-C paired 500-iter feasibility probe) precede any launch. Ten [DECISION-REQUIRED] items await the user, incl. whether a DGX-trained teacher may be deployed (the +109% machine-isolation caveat rests on one same-seed pair inside the 56% seed floor).

## finding
- `finding/001` [subject: 32768-envs-fit-on-the-dgx-gb10-83-2-121-7-gb-peak-no-fallbac] 32768 envs FIT on the DGX GB10 (83.2/121.7 GB peak, no fallback) at 34.73 s/iter, but per-doubling cost is degrading (1.78x -> 1.90x) and a 200-iter probe cannot see DORAEMON at all (step_interval 250) — 32768 envs FIT on the DGX GB10 (83.2/121.7 GB peak, no fallback) at 34.73 s/iter, but per-doubling cost is degrading (1.78x -> 1.90x) and a 200-iter probe cannot see DORAEMON at all (step_interval 250)
- `finding/005` [subject: a-distillation-run-is-invisible-to-every-teacher-run-instrum] A distillation run is invisible to every teacher-run instrument: wrong tree, no Learning-iteration line, buffered stdout — A distillation run is invisible to every teacher-run instrument: wrong tree, no Learning-iteration line, buffered stdout
- `finding/007` [subject: a-random-expansion-lift-is-inert-on-the-albc-plant-while-a-l] A random expansion lift is inert on the ALBC plant while a learned dictionary is not, and the learned one saturates at about 12 effective dimensions — A random expansion lift is inert on the ALBC plant while a learned dictionary is not, and the learned one saturates at about 12 effective dimensions
- `finding/010` [subject: a-stateful-observation-component-cannot-assume-get-observati] A stateful observation component cannot assume _get_observations runs once per step: the teacher runner calls it an extra time per iteration — A stateful observation component cannot assume _get_observations runs once per step: the teacher runner calls it an extra time per iteration
- `finding/011` [subject: absolute-degree-reference-scales-for-judging-attitude-deltas] Absolute-degree reference scales for judging attitude deltas (stop reading percentages on a small base) — Absolute-degree reference scales for judging attitude deltas (stop reading percentages on a small base)
- `finding/013` [subject: action-pipeline-behavior-walk-through-two-clamps-raw-gaussia] action pipeline behavior walk-through (two clamps / raw gaussian vs tanh / delta integrator reset / single-vector two-dynamics / arm PD gain DR) — action pipeline behavior walk-through (two clamps / raw gaussian vs tanh / delta integrator reset / single-vector two-dynamics / arm PD gain DR)
- `finding/014` [subject: actuator-hardware-identification-arm-xw540-t260-board-measur] Actuator hardware identification: arm=XW540-T260 (board-measured), prior 'no PID-gain code' claim corrected, thrust-curve keep-off, step-response prereqs — Actuator hardware identification: arm=XW540-T260 (board-measured), prior 'no PID-gain code' claim corrected, thrust-curve keep-off, step-response prereqs
- `finding/015` [subject: added-mass-scale-upper-half-is-attenuated-not-inert-the-0-95] added_mass_scale upper half is ATTENUATED, not inert: the 0.95*I clamp ceiling is co-sampled with inertia_scale DR; still do not widen hydro DR up — added_mass_scale upper half is ATTENUATED, not inert: the 0.95*I clamp ceiling is co-sampled with inertia_scale DR; still do not widen hydro DR up
- `finding/017` [subject: albc-control-hz-re-verdict-from-three-undocumented-2026-08-2] albc control_hz re-verdict from three undocumented 2026-08-25 runs recovered from bags: 10 Hz was the quietest of the night (yaw span 4.0 deg, roll std 2.9, pitch std 0.36 over 40 s, joint rate unsaturated), 100 Hz diverged in 4 s (policy pushed the arm first, then body), and observation rate and control_hz are not separable in this data — albc control_hz re-verdict from three undocumented 2026-08-25 runs recovered from bags: 10 Hz was the quietest of the night (yaw span 4.0 deg, roll std 2.9, pitch std 0.36 over 40 s, joint rate unsaturated), 100 Hz diverged in 4 s (policy pushed the arm first, then body), and observation rate and control_hz are not separable in this data
- `finding/018` [subject: albc-field-2026-08-25-night-arm-only-attitude-tracking-reach] albc field 2026-08-25 night: arm-only attitude tracking reaches the command (cmd 30 deg gives body roll +28.7 deg); the fold singularity does not amplify jitter (+12 pct) but kills the signal (attainable/ripple 3.4 vs 10.7; cmd (30,30) held 58 s at theta2 = fold-13 deg gave +2.8 deg) — albc field 2026-08-25 night: arm-only attitude tracking reaches the command (cmd 30 deg gives body roll +28.7 deg); the fold singularity does not amplify jitter (+12 pct) but kills the signal (attainable/ripple 3.4 vs 10.7; cmd (30,30) held 58 s at theta2 = fold-13 deg gave +2.8 deg)
- `finding/019` [subject: albc-jitter-is-a-closed-loop-limit-cycle-at-0-36-0-74-hz-han] albc jitter is a closed-loop limit cycle at 0.36-0.74 Hz (Hankel-DMD |lambda| 0.997-1.001 complex pairs in every closed-loop window, none in open-loop), open-loop arm-step ripple is 0.04-0.20 deg; delay-dominated vs rate-saturation-dominated is undecided — albc jitter is a closed-loop limit cycle at 0.36-0.74 Hz (Hankel-DMD |lambda| 0.997-1.001 complex pairs in every closed-loop window, none in open-loop), open-loop arm-step ripple is 0.04-0.20 deg; delay-dominated vs rate-saturation-dominated is undecided
- `finding/020` [subject: albc-open-loop-arm-attitude-dc-gain-recovered-from-bag-j2-ga] albc open-loop arm attitude DC gain recovered from bag j2_gain_0825 in body frame: 148 deg per m of EE radius, 32 deg/rad of theta2 (n=6, 3 pct spread), matching the closed-loop static map (+145 deg/m, R^2 0.998); the retracted 1.2 deg / 25x-short-of-sim claim is off by 65x and retraining is not justified by arm authority — albc open-loop arm attitude DC gain recovered from bag j2_gain_0825 in body frame: 148 deg per m of EE radius, 32 deg/rad of theta2 (n=6, 3 pct spread), matching the closed-loop static map (+145 deg/m, R^2 0.998); the retracted 1.2 deg / 25x-short-of-sim claim is off by 65x and retraining is not justified by arm authority
- `finding/023` [subject: an-off-doraemon-channel-that-costs-return-stalls-the-curricu] an off-DORAEMON channel that costs return stalls the curriculum below the alpha floor (mode -2 entire run) — an off-DORAEMON channel that costs return stalls the curriculum below the alpha floor (mode -2 entire run)
- `finding/024` [subject: an-r2-delta-between-two-runs-mixes-error-change-with-denomin] An R2 DELTA between two runs mixes error change with denominator change whenever their eval env draws differ -- decompose it before reporting the headline — An R2 DELTA between two runs mixes error change with denominator change whenever their eval env draws differ -- decompose it before reporting the headline
- `finding/025` [subject: analysis-engine-map-what-is-grow-able-vs-off-limits] analysis engine map — what is grow-able vs off-limits — analysis engine map — what is grow-able vs off-limits
- `finding/031` [subject: at-the-fitted-slope-model-13400-would-need-roughly-4000-furt] At the fitted slope, `model_13400` would need roughly 4000 further iterations to — At the fitted slope, `model_13400` would need roughly 4000 further iterations to
- `finding/033` [subject: attitude-only-per-axis-cv-roll-drives-the-heavy-tail-crossov] attitude_only per-axis CV: roll DRIVES the heavy-tail crossover, yaw is the EXTREME tail — attitude_only per-axis CV: roll DRIVES the heavy-tail crossover, yaw is the EXTREME tail
- `finding/034` [subject: barrier-alpha-runtime-0-05-agent-cfg-injects-over-ctor-defau] barrier_alpha runtime=0.05 (agent cfg injects over ctor default 0.02) — barrier_alpha runtime=0.05 (agent cfg injects over ctor default 0.02)
- `finding/035` [subject: baseline-open-experiment-leads-backlog-beyond-heavy-tail-tri] Baseline open experiment-leads backlog (beyond heavy-tail): triage by value x launchability with blockers — Baseline open experiment-leads backlog (beyond heavy-tail): triage by value x launchability with blockers
- `finding/036` [subject: baseline-repro-reproduces-teacher-deterministically-e4-contr] baseline-repro reproduces teacher deterministically (E4 control) — baseline-repro reproduces teacher deterministically (E4 control)
- `finding/037` [subject: before-citing-an-eval-verify-its-run-actually-trained-manife] Before citing an eval, verify its run actually trained: manifest status, log directory, wandb, checkpoint count — Before citing an eval, verify its run actually trained: manifest status, log directory, wandb, checkpoint count
- `finding/038` [subject: bias-ema-obs-is-deployment-safe-computed-from-command-minus-] bias_ema obs is deployment-safe: computed from command minus measured attitude, no privileged state — bias_ema obs is deployment-safe: computed from command minus measured attitude, no privileged state
- `finding/042` [subject: buoy-added-mass-is-wrong-in-both-sims-and-in-opposite-direct] Buoy added mass is wrong in BOTH sims and in opposite directions: Isaac 3.8-8.2x below the geometric value, Stonefish 3.5-5.7x above it via an isotropic average of a dimensionally broken axial term; recentering Isaac onto Stonefish moves it further from reality — Buoy added mass is wrong in BOTH sims and in opposite directions: Isaac 3.8-8.2x below the geometric value, Stonefish 3.5-5.7x above it via an isotropic average of a dimensionally broken axial term; recentering Isaac onto Stonefish moves it further from reality
- `finding/043` [subject: buoyancy-gravity-restoring-apply-separately-to-main-body-vs-] buoyancy/gravity/restoring apply SEPARATELY to main body vs buoy(link3); gravity HAS DR (body_mass_scale+payload_mass); DR is body-shared not independent — buoyancy/gravity/restoring apply SEPARATELY to main body vs buoy(link3); gravity HAS DR (body_mass_scale+payload_mass); DR is body-shared not independent
- `finding/045` [subject: c4b-dagger-correction-measured-partial-2-5-4x-in-loop-reduct] C4b DAgger correction measured: partial (2.5-4x in-loop reduction at low-mod DR, under-dispersion floor persists at hard) — C4b DAgger correction measured: partial (2.5-4x in-loop reduction at low-mod DR, under-dispersion floor persists at hard)
- `finding/047` [subject: campaign-wide-latent-r2-census-11-student-arms-negative-r2-i] Campaign-wide latent R2 census (11 student arms): negative R2 is NOT the campaign norm, the gen-1 extra-obs arm is the best tracker, and the obs76 gen-2 student sits at the bottom while being the only arm that kills envs — Campaign-wide latent R2 census (11 student arms): negative R2 is NOT the campaign norm, the gen-1 extra-obs arm is the best tracker, and the obs76 gen-2 student sits at the bottom while being the only arm that kills envs
- `finding/050` [subject: command-is-not-domain-randomization-command-range-scale-is-i] command is NOT domain randomization; command range scale is inert residue (unwired 2026-04-06, stale comment) — command is NOT domain randomization; command range scale is inert residue (unwired 2026-04-06, stale comment)
- `finding/052` [subject: constraint-budget-x0-5-binds-only-thruster-util-authority-st] constraint budget x0.5 binds only thruster_util -> authority starvation — constraint budget x0.5 binds only thruster_util -> authority starvation
- `finding/053` [subject: constraint-health-index] constraint health index — constraint health index
- `finding/054` [subject: constraint-margin-must-be-normalized-j-c-d-k-absolute-margin] constraint margin must be normalized (J_C/d_k) -- absolute margin flips binding/slack — constraint margin must be normalized (J_C/d_k) -- absolute margin flips binding/slack
- `finding/056` [subject: constraint-viol-trajectory-barrier-sign-metrics-4-10-starts-] Constraint/viol trajectory + barrier sign + metrics 4->10: starts at -d_k rising=healthy, barrier mostly-negative=healthy, early-satisfy tail=over-loose candidate — Constraint/viol trajectory + barrier sign + metrics 4->10: starts at -d_k rising=healthy, barrier mostly-negative=healthy, early-satisfy tail=over-loose candidate
- `finding/057` [subject: constrainttrpo-faithful-norbc-modified-ipo-kim-2024-arxiv-23] ConstraintTRPO = faithful NORBC Modified-IPO (Kim 2024, arXiv:2308.12517); standardized-vs-raw barrier + soft feasibility are NORBC design not bugs; only the 1e-8 barrier clamp is code-level (no functional fix) — ConstraintTRPO = faithful NORBC Modified-IPO (Kim 2024, arXiv:2308.12517); standardized-vs-raw barrier + soft feasibility are NORBC design not bugs; only the 1e-8 barrier clamp is code-level (no functional fix)
- `finding/058` [subject: constrainttrpo-slack-tail-9-of-10-constraints-deep-slack-is-] ConstraintTRPO slack tail: 9 of 10 constraints deep-slack is plausibly healthy complementary slackness (UNVERIFIED) -- confirm via training-time trajectory + loosening ablation before tuning budgets — ConstraintTRPO slack tail: 9 of 10 constraints deep-slack is plausibly healthy complementary slackness (UNVERIFIED) -- confirm via training-time trajectory + loosening ablation before tuning budgets
- `finding/065` [subject: deploy-export-was-tcn-only-so-it-shipped-the-non-adopted-stu] Deploy export was TCN-only so it shipped the non-adopted student arm; StudentGRUSpec closes GRU parity (latent 1.2e-07, hidden 1.6e-07 over 9 steps) and rejects shallow-head / multi-layer geometries — Deploy export was TCN-only so it shipped the non-adopted student arm; StudentGRUSpec closes GRU parity (latent 1.2e-07, hidden 1.6e-07 over 9 steps) and rejects shallow-head / multi-layer geometries
- `finding/066` [subject: detached-isaaclab-sh-launches-die-instantly-with-ansi-tabs-u] Detached isaaclab.sh launches die instantly with ansi+tabs unknown terminal type unless TERM is set -- and every health probe reads it as a clean finish — Detached isaaclab.sh launches die instantly with ansi+tabs unknown terminal type unless TERM is set -- and every health probe reads it as a clean finish
- `finding/067` [subject: diagnosis-md-2026-05-25-retirement-open-item-ledger] DIAGNOSIS.md 2026-05-25 retirement: open-item ledger — DIAGNOSIS.md 2026-05-25 retirement: open-item ledger
- `finding/068` [subject: diagnosis-the-eval-regression-at-9000-has-no-counterpart-in-] DIAGNOSIS — the eval regression at 9000 has no counterpart in the reward signal. — DIAGNOSIS — the eval regression at 9000 has no counterpart in the reward signal.
- `finding/070` [subject: doraemon-becomes-feasibility-limited-at-the-ceiling-the-kl-u] DORAEMON becomes feasibility-limited at the ceiling: the kl_ub 0.06 arm reached 98.6 percent of full DR then contracted because mean_reward sits below performance_lb — DORAEMON becomes feasibility-limited at the ceiling: the kl_ub 0.06 arm reached 98.6 percent of full DR then contracted because mean_reward sits below performance_lb
- `finding/071` [subject: doraemon-curriculum-saturation-is-iteration-clocked-not-env-] DORAEMON curriculum saturation is iteration-clocked, not env-clocked: 4x num_envs (4096->16384) moved Gate A by only 250 of ~7000 iterations — DORAEMON curriculum saturation is iteration-clocked, not env-clocked: 4x num_envs (4096->16384) moved Gate A by only 250 of ~7000 iterations
- `finding/072` [subject: doraemon-difficulty-has-3-separable-levers-kl-ub-step-size-s] DORAEMON difficulty has 3 separable levers: kl_ub (step size), step_interval (dwell-time), max_iterations (number of expansions) — DORAEMON difficulty has 3 separable levers: kl_ub (step size), step_interval (dwell-time), max_iterations (number of expansions)
- `finding/073` [subject: doraemon-entropy-opt-rejected-is-a-warning-price-it-from-cur] DORAEMON Entropy opt rejected is a WARNING: price it from curriculum_trajectory.json, not the log (successful expansions are never logged) — DORAEMON Entropy opt rejected is a WARNING: price it from curriculum_trajectory.json, not the log (successful expansions are never logged)
- `finding/074` [subject: doraemon-is-trust-region-limited-not-feasibility-limited-kl-] DORAEMON is trust-region-limited not feasibility-limited: kl_step is pinned AT kl_ub every update, so kl_ub x n_updates is one expansion budget — DORAEMON is trust-region-limited not feasibility-limited: kl_step is pinned AT kl_ub every update, so kl_ub x n_updates is one expansion budget
- `finding/075` [subject: doraemon-kl-step-final-value-0-0000-is-a-sparse-logging-arti] DORAEMON/kl_step final value 0.0000 is a sparse-logging artifact, NOT a frozen curriculum — DORAEMON/kl_step final value 0.0000 is a sparse-logging artifact, NOT a frozen curriculum
- `finding/077` [subject: doraemon-over-widens-then-oscillates-when-a-converged-teache] DORAEMON over-widens then oscillates when a converged teacher is given extra budget (chases alpha floor, overshoots, contracts) — DORAEMON over-widens then oscillates when a converged teacher is given extra budget (chases alpha floor, overshoots, contracts)
- `finding/079` [subject: doraemon-success-rate-is-the-only-training-side-metric-that-] `DORAEMON/success_rate` is the only training-side metric that tracks the eval ex — `DORAEMON/success_rate` is the only training-side metric that tracks the eval ex
- `finding/080` [subject: dr-curriculum-is-still-opening-at-iter-5000-dr-difficulty-is] DR curriculum is still opening at iter 5000 -- DR difficulty is iteration-limited, not bounds-limited — DR curriculum is still opening at iter 5000 -- DR difficulty is iteration-limited, not bounds-limited
- `finding/083` [subject: dr-harder-reward-decomposition-confirms-eval-trades-on-the-t] dr-harder: reward decomposition confirms eval trades on the training side — dr-harder: reward decomposition confirms eval trades on the training side
- `finding/084` [subject: e0-eval-latent-instrument-fix-38d979e-lives-only-on-the-stud] E0 eval latent instrument fix 38d979e lives only on the student branch, not main — E0 eval latent instrument fix 38d979e lives only on the student branch, not main
- `finding/087` [subject: e5-alpha075-ood-per-axis-generalization-gap-universal-failur] E5 alpha075 OOD per-axis generalization gap (universal failure = roll DC-bias doubles OOD) — E5 alpha075 OOD per-axis generalization gap (universal failure = roll DC-bias doubles OOD)
- `finding/088` [subject: e-int-s-stitched-resume-offset-the-doraemon-update-grid-its-] E-int's stitched resume offset the DORAEMON update grid: its fault_severity reads 0.0770 iteration-matched (iter 4749) but 0.0901 at actual run end (iter 4999) — E-int's stitched resume offset the DORAEMON update grid: its fault_severity reads 0.0770 iteration-matched (iter 4749) but 0.0901 at actual run end (iter 4999)
- `finding/090` [subject: encoder-health-is-clean-and-is-not-the-regression-s-origin-z] Encoder health is clean and is not the regression's origin: `z_std` 0.389-0.394 — Encoder health is clean and is not the regression's origin: `z_std` 0.389-0.394
- `finding/091` [subject: encoder-latent-z-dim-ablation-coupling-points-constraints-fo] Encoder latent z_dim ablation: coupling points + constraints for a future sweep (from-scratch, student sync, verify via z_sweep) — Encoder latent z_dim ablation: coupling points + constraints for a future sweep (from-scratch, student sync, verify via z_sweep)
- `finding/092` [subject: encoder-priv-obs-normalization-bounds-must-be-dr-derived-not] encoder priv-obs normalization bounds must be DR-derived, not hardcoded (silent drift bug) — encoder priv-obs normalization bounds must be DR-derived, not hardcoded (silent drift bug)
- `finding/093` [subject: engine-gap-analyze-training-deep-plot-has-2-blank-panels-inc] ENGINE-GAP: analyze_training deep-plot has 2 blank panels + inconsistent legends; no DORAEMON curriculum plot — ENGINE-GAP: analyze_training deep-plot has 2 blank panels + inconsistent legends; no DORAEMON curriculum plot
- `finding/112` [subject: engine-generic-flags-entropy-collapse-barrier-spike-reward-p] engine generic flags (entropy-collapse / barrier-spike / reward-plateau) are benign for a converged teacher — engine generic flags (entropy-collapse / barrier-spike / reward-plateau) are benign for a converged teacher
- `finding/113` [subject: esc-deadband-is-1450-1545-us-on-this-uuv-the-vertical-channe] ESC deadband is 1450..1545 us on this UUV: the vertical channels parked inside it, and thruster_order was wrong for three horizontals — ESC deadband is 1450..1545 us on this UUV: the vertical channels parked inside it, and thruster_order was wrong for three horizontals
- `finding/116` [subject: eval-decision-floors-are-the-binding-standard-for-student-ar] Eval decision floors are the binding standard for student-arm comparisons (0.1 deg / 15 envs) — Eval decision floors are the binding standard for student-arm comparisons (0.1 deg / 15 envs)
- `finding/121` [subject: eval-py-static-ood-appends-a-fifth-dr-level-and-unpairs-ever] eval.py static --ood appends a fifth DR level and unpairs every cross-run comparison at soft/medium/hard — eval.py static --ood appends a fifth DR level and unpairs every cross-run comparison at soft/medium/hard
- `finding/122` [subject: eval-py-student-mode-restores-the-extra-obs-env-config-for-g] eval.py student-mode restores the extra-obs env config for GEN-1 only: a gen-2 student carries extra_obs_dim=0, so the restore block is skipped and env.use_extra_policy_obs=True must be passed explicitly or the env builds 72D against a 76D student — eval.py student-mode restores the extra-obs env config for GEN-1 only: a gen-2 student carries extra_obs_dim=0, so the restore block is skipped and env.use_extra_policy_obs=True must be passed explicitly or the env builds 72D against a 76D student
- `finding/124` [subject: every-5000-iteration-teacher-on-the-buoyfix-plant-stops-at-6] Every 5000-iteration teacher on the buoyfix plant stops at ~65 percent of its DR curriculum, so the declared DR box is NOT the box it trained against — Every 5000-iteration teacher on the buoyfix plant stops at ~65 percent of its DR curriculum, so the declared DR box is NOT the box it trained against
- `finding/131` [subject: exploration-is-not-coupled-to-curriculum-width-dr-grew-10-29] Exploration is not coupled to curriculum width: DR grew 10-29x while sigma shrank 9 percent, and the three obvious fixes are already refuted here — Exploration is not coupled to curriculum width: DR grew 10-29x while sigma shrank 9 percent, and the three obvious fixes are already refuted here
- `finding/133` [subject: extend8k-saturated-the-dr-config-box-at-iter-7000-all-20-par] extend8k SATURATED the DR config box at iter 7000 (all 20 params Beta(1,1) = uniform): performance_lb/kl_ub were never the limit, so its `hard` exam IS absolute -- but the 5000-run's is not — extend8k SATURATED the DR config box at iter 7000 (all 20 params Beta(1,1) = uniform): performance_lb/kl_ub were never the limit, so its `hard` exam IS absolute -- but the 5000-run's is not
- `finding/134` [subject: extra-obs-dim-0-shifts-the-eval-rng-stream-so-an-obs4-arm-ne] extra_obs_dim>0 shifts the eval RNG stream so an obs4 arm never shares env draws with a pre-obs4 run, but a dim=0 control on the same commit is byte-identical to one — extra_obs_dim>0 shifts the eval RNG stream so an obs4 arm never shares env draws with a pre-obs4 run, but a dim=0 control on the same commit is byte-identical to one
- `finding/136` [subject: final-quality-past-saturation-remains-open-in-one-narrow-sen] Final quality past saturation remains open in one narrow sense only: the run was — Final quality past saturation remains open in one narrow sense only: the run was
- `finding/137` [subject: fitting-a-koopman-with-control-operator-on-deterministic-on-] Fitting a Koopman-with-control operator on deterministic on-policy rollouts cannot identify B, and a richer dictionary makes it worse — Fitting a Koopman-with-control operator on deterministic on-policy rollouts cannot identify B, and a richer dictionary makes it worse
- `finding/138` [subject: four-candidate-paper-stories-for-the-albc-line-synthesised-f] Four candidate paper stories for the ALBC line, synthesised from 55 substantive wiki decisions 2026-08-05 — Four candidate paper stories for the ALBC line, synthesised from 55 substantive wiki decisions 2026-08-05
- `finding/139` [subject: frontier-seeking-dr-curriculum-literature-survey-adr-currot-] Frontier-seeking DR curriculum literature survey (ADR/CURROT/ACCEL) and why naive success-oscillation is unsound — Frontier-seeking DR curriculum literature survey (ADR/CURROT/ACCEL) and why naive success-oscillation is unsound
- `finding/141` [subject: ftc-investigation-2026-07-25-m4-loss-halves-pure-yaw-ceiling] FTC investigation 2026-07-25: m4 loss halves pure-yaw ceiling (util x2) while roll/pitch stay buoyancy-dominated; literature + composition risks; verdict MEASURE-FIRST (deterministic m4-kill eval before any FTC training) — FTC investigation 2026-07-25: m4 loss halves pure-yaw ceiling (util x2) while roll/pitch stay buoyancy-dominated; literature + composition risks; verdict MEASURE-FIRST (deterministic m4-kill eval before any FTC training)
- `finding/142` [subject: gate-a-closed-at-iteration-7250-all-29-expansions-were-accep] Gate A closed at iteration 7250. All 29 expansions were accepted at the KL cap 0 — Gate A closed at iteration 7250. All 29 expansions were accepted at the KL cap 0
- `finding/146` [subject: halving-kl-ub-cost-1-42x-iterations-to-saturation-not-the-2x] Halving kl_ub cost 1.42x iterations to saturation, not the 2x the trust-region model predicts -- and trajectory retention blocks closing the confound — Halving kl_ub cost 1.42x iterations to saturation, not the 2x the trust-region model predicts -- and trajectory retention blocks closing the confound
- `finding/147` [subject: heavy-tail-vs-sample-mean-divergence-are-independent] heavy-tail vs sample-mean divergence are independent — heavy-tail vs sample-mean divergence are independent
- `finding/148` [subject: hmm-regime-count-separates-dr-harder-runs-overfit-vs-healthy] HMM regime count separates dr-harder runs (overfit vs healthy) — HMM regime count separates dr-harder runs (overfit vs healthy)
- `finding/150` [subject: hydro-dr-train-eval-sampling-mismatch-is-real-but-left-as-is] hydro DR train/eval sampling mismatch is REAL but left as-is (option C): train scalar->6-axis broadcast, eval 6-axis independent — hydro DR train/eval sampling mismatch is REAL but left as-is (option C): train scalar->6-axis broadcast, eval 6-axis independent
- `finding/152` [subject: hydrorc-is-half-recentered-buoy-link3-nominals-untouched-but] HydroRC IS half-recentered (buoy/link3 nominals untouched) -- but the '10x under added mass' framing dies to the effective-vs-effective correction (~2.4x); the lead survives on a different mechanism: HydroRC drops hull yaw damping 45x, so unmeasured analytical buoy damping becomes 1.8x hull's and DOMINATES the retrained plant — HydroRC IS half-recentered (buoy/link3 nominals untouched) -- but the '10x under added mass' framing dies to the effective-vs-effective correction (~2.4x); the lead survives on a different mechanism: HydroRC drops hull yaw damping 45x, so unmeasured analytical buoy damping becomes 1.8x hull's and DOMINATES the retrained plant
- `finding/154` [subject: imu-45deg-offset-pitch-negation-sim-uncompensated-2026-07-05] IMU 45deg mounting offset + pitch negation is sim-uncompensated (2026-07-05) — IMU 45deg mounting offset + pitch negation is sim-uncompensated (2026-07-05)
- `finding/157` [subject: joint1-anti-drift-design-history] joint1 anti-drift design history — joint1 anti-drift design history
- `finding/158` [subject: joint1-centering-reward-is-removed-on-main-6-term-but-alive-] joint1_centering reward is REMOVED on main (6-term) but ALIVE on exp/latency-dr (7-term); reward.md doc is main-stale — joint1_centering reward is REMOVED on main (6-term) but ALIVE on exp/latency-dr (7-term); reward.md doc is main-stale
- `finding/159` [subject: joint1-cumulative-ipo-constraint-generalizes-drift-bounded-a] joint1_cumulative IPO constraint generalizes (drift bounded at OOD) while the attitude tracker does not (roll OOD heavy-tail) — joint1_cumulative IPO constraint generalizes (drift bounded at OOD) while the attitude tracker does not (roll OOD heavy-tail)
- `finding/160` [subject: joint1-cumulative-rotation-constraint-never-binds-policy-par] joint1 cumulative-rotation constraint never binds: policy parks at ~0.36 rev, +-4pi is a dead-zone rail — joint1 cumulative-rotation constraint never binds: policy parks at ~0.36 rev, +-4pi is a dead-zone rail
- `finding/162` [subject: joint-dr-params-kp-kd-effort-friction-need-no-dedicated-meas] Joint DR params (Kp/Kd/effort/friction) need NO dedicated measurement: PD-gain center already measured, effort/friction are DR-bypass by design — Joint DR params (Kp/Kd/effort/friction) need NO dedicated measurement: PD-gain center already measured, effort/friction are DR-bypass by design
- `finding/163` [subject: joint-target-runaway-is-not-a-sim-to-real-gap-both-sides-unb] Joint target runaway is NOT a sim-to-real gap (both sides unbounded); the real asymmetry is the accumulator reset — Joint target runaway is NOT a sim-to-real gap (both sides unbounded); the real asymmetry is the accumulator reset
- `finding/164` [subject: k0-theta-probe-2026-08-04-the-teacher-latent-z-recovers-ever] K0 theta-probe 2026-08-04: the teacher latent z recovers every plant parameter it was GIVEN (mass/geometry, R2 up to 0.49) and none that was withheld -- damping and 5 of 6 added-mass DOF are at the shuffle floor because they are absent from p_t, not because the encoder failed — K0 theta-probe 2026-08-04: the teacher latent z recovers every plant parameter it was GIVEN (mass/geometry, R2 up to 0.49) and none that was withheld -- damping and 5 of 6 added-mass DOF are at the shuffle floor because they are absent from p_t, not because the encoder failed
- `finding/165` [subject: k2-replication-2026-08-04-the-one-step-least-squares-operato] K2 replication 2026-08-04: the one-step least-squares operator on the teacher latent is the identity to within refit noise across 5 run-level pairs, and the reason is structural -- z encodes per-episode CONSTANT parameters — K2 replication 2026-08-04: the one-step least-squares operator on the teacher latent is the identity to within refit noise across 5 run-level pairs, and the reason is structural -- z encodes per-episode CONSTANT parameters
- `finding/166` [subject: kl-ub-0-12-trades-attitude-for-translation-e1-dr-harder] kl_ub 0.12 trades attitude for translation (E1 dr-harder) — kl_ub 0.12 trades attitude for translation (E1 dr-harder)
- `finding/170` [subject: koopman-rl-integration-research-consolidated-reference-2026-] Koopman RL integration research consolidated reference 2026-08-03 — Koopman RL integration research consolidated reference 2026-08-03
- `finding/172` [subject: latent-dim-d4-collapses-at-none-dr-in-every-student-arm-and-] Latent dim d4 collapses at none-DR in every student arm and the blanket none-exclusion hides it — Latent dim d4 collapses at none-DR in every student arm and the blanket none-exclusion hides it
- `finding/173` [subject: leaky-integral-and-ema-bias-carry-over-the-mid-episode-comma] Leaky-integral and EMA-bias carry over the mid-episode command resample (unexamined side-effect, harm unproven, A/B design ready) — Leaky-integral and EMA-bias carry over the mid-episode command resample (unexamined side-effect, harm unproven, A/B design ready)
- `finding/174` [subject: literature-map-how-rl-control-actually-handles-steady-state-] Literature map: how RL control actually handles steady-state error (cross-domain) -- nobody has eliminated it; our r_bias reward penalty is a single-paper idea, our encoder is the field's top-ranked mechanism with no UUV precedent — Literature map: how RL control actually handles steady-state error (cross-domain) -- nobody has eliminated it; our r_bias reward penalty is a single-paper idea, our encoder is the field's top-ranked mechanism with no UUV precedent
- `finding/175` [subject: metrics-yaml-declares-doraemon-success-rate-but-the-real-tb-] metrics.yaml declares doraemon_success_rate but the real TB tag is DORAEMON/success_rate -- a coverage check that trusts the declared token reports the group as unlogged — metrics.yaml declares doraemon_success_rate but the real TB tag is DORAEMON/success_rate -- a coverage check that trusts the declared token reports the group as unlogged
- `finding/176` [subject: model-13400-is-the-only-checkpoint-with-zero-ood-failures-0-] `model_13400` is the only checkpoint with zero OOD failures (0/64 vs 1/64 at 750 — `model_13400` is the only checkpoint with zero OOD failures (0/64 vs 1/64 at 750
- `finding/178` [subject: neither-the-entropy-nor-the-sigma-trajectory-has-an-inflecti] Neither the entropy nor the sigma trajectory has an inflection at the regression — Neither the entropy nor the sigma trajectory has an inflection at the regression
- `finding/181` [subject: no-constraint-was-ever-violated-every-viol-tag-is-the-exact-] No constraint was ever violated. Every `viol` tag is the exact negation of its m — No constraint was ever violated. Every `viol` tag is the exact negation of its m
- `finding/182` [subject: no-divergence-signature-at-any-point-loss-kl-attains-its-max] No divergence signature at any point. `Loss/kl` attains its maximum at its first — No divergence signature at any point. `Loss/kl` attains its maximum at its first
- `finding/183` [subject: none-level-invariance-holds-within-a-machine-not-across-them] none-level invariance holds within a machine, not across them: the same checkpoint moved 4 percent when re-scored on a different GPU, while a same-machine control reproduced exactly — none-level invariance holds within a machine, not across them: the same checkpoint moved 4 percent when re-scored on a different GPU, while a same-machine control reproduced exactly
- `finding/187` [subject: obs-noise-model-history-channels-are-re-noised-every-step-si] Obs-noise model: history channels are re-noised every step (sim past is not frozen like hardware) — Obs-noise model: history channels are re-noised every step (sim past is not frozen like hardware)
- `finding/188` [subject: ocean-nominal-shift-collapses-actor-entropy-e2-dr-harder] ocean nominal shift collapses actor entropy (E2 dr-harder) — ocean nominal shift collapses actor entropy (E2 dr-harder)
- `finding/193` [subject: on-the-albc-plant-the-binding-prediction-error-is-transfer-t] On the ALBC plant the binding prediction error is transfer to held-out plants, not model class, so lifting does not touch the dominant term — On the ALBC plant the binding prediction error is transfer to held-out plants, not model class, so lifting does not touch the dominant term
- `finding/194` [subject: on-the-albc-plant-the-linear-evolution-constraint-reaches-co] On the ALBC plant the linear-evolution constraint reaches control and costs it: arm C is beaten by its own nonlinear twin in 51 of 72 cells — On the ALBC plant the linear-evolution constraint reaches control and costs it: arm C is beaten by its own nonlinear twin in 51 of 72 cells
- `finding/195` [subject: onboard-measured-2026-07-06-arm-step-response-valid-sim-zeta] Onboard measured 2026-07-06: arm step-response (VALID, sim zeta~0.7 matches) + net buoyancy (NEUTRAL confirmed, sign unresolvable at 1cm depth quantization) — Onboard measured 2026-07-06: arm step-response (VALID, sim zeta~0.7 matches) + net buoyancy (NEUTRAL confirmed, sign unresolvable at 1cm depth quantization)
- `finding/196` [subject: ood-does-not-invert-the-in-distribution-ranking-it-dissolves] OOD does not invert the in-distribution ranking, it dissolves it. `model_7500` b — OOD does not invert the in-distribution ranking, it dissolves it. `model_7500` b
- `finding/198` [subject: open-on-land-the-policy-winds-j2-to-pi-and-beyond-and-the-bo] OPEN: on land the policy winds J2 to pi and beyond, and the board feeds it IMU at 20 Hz / joints at 10 Hz against a 50 Hz control loop — OPEN: on land the policy winds J2 to pi and beyond, and the board feeds it IMU at 20 Hz / joints at 10 Hz against a 50 Hz control loop
- `finding/199` [subject: our-doraemon-original-both-entropy-max-center-can-move-kl-ub] Our DORAEMON == original (both entropy-max); center CAN move, kl_ub is the speed bottleneck — Our DORAEMON == original (both entropy-max); center CAN move, kl_ub is the speed bottleneck
- `finding/203` [subject: penalty-vs-objective-exchange-rate-deg-of-attitude-error-bou] Penalty-vs-objective exchange rate (deg of attitude error bought per penalty term) is the rescaling evidence, not the 1% reward share; all four penalties together buy only 0.125 deg — Penalty-vs-objective exchange rate (deg of attitude error bought per penalty term) is the rescaling evidence, not the 1% reward share; all four penalties together buy only 0.125 deg
- `finding/204` [subject: per-env-heavy-tail-analysis-current-capability-hard-ceiling-] per-env heavy-tail analysis: current capability, hard ceiling, and what fault-tolerant-control research needs added — per-env heavy-tail analysis: current capability, hard ceiling, and what fault-tolerant-control research needs added
- `finding/207` [subject: performance-lb-recon-needs-zero-new-rollouts-doraemon-state-] performance_lb recon needs ZERO new rollouts: doraemon_state.pt already carries buffer_returns (the 260608 p25 method) — performance_lb recon needs ZERO new rollouts: doraemon_state.pt already carries buffer_returns (the 260608 p25 method)
- `finding/211` [subject: policy-head-design-rationale-and-literature-standing-encoder] policy head design rationale and literature standing (encoder input / softsign / obs norm / std / entropy) — policy head design rationale and literature standing (encoder input / softsign / obs norm / std / entropy)
- `finding/212` [subject: posttam-constraint-set-is-healthy-8-active-guards-exactly-2-] Posttam constraint set is healthy: 8 active guards + exactly 2 dormant (rp_vel_settling, manipulability) — Posttam constraint set is healthy: 8 active guards + exactly 2 dormant (rp_vel_settling, manipulability)
- `finding/213` [subject: posttam-encoder-is-alive-and-specialized-z-sweep-exploration] Posttam encoder is alive and specialized (z-sweep) — exploration collapse is a converged optimum, not a dead encoder — Posttam encoder is alive and specialized (z-sweep) — exploration collapse is a converged optimum, not a dead encoder
- `finding/214` [subject: priv-obs-slim-stage-2-lead-contested-p-t-dims-quad-damp-lin-] priv-obs slim Stage-2 lead: contested p_t dims (quad_damp, lin_vel) need WITH-vs-WITHOUT A/B; union kept them — priv-obs slim Stage-2 lead: contested p_t dims (quad_damp, lin_vel) need WITH-vs-WITHOUT A/B; union kept them
- `finding/215` [subject: profile] Workspace profile (auto-synced) — Workspace profile (auto-synced)
- `finding/217` [subject: real-albc-deployment-state-estimation-rates-measured-from-co] Real ALBC deployment state-estimation rates measured from code: attitude+gyro at most ~25 Hz (loop_speed/4, self-telemetered in the sensors DEPTH field), joints 10 Hz, control 50 Hz -- the real policy runs on zero-order-held stale observations, so 50 Hz Stonefish odom is already faster than reality — Real ALBC deployment state-estimation rates measured from code: attitude+gyro at most ~25 Hz (loop_speed/4, self-telemetered in the sensors DEPTH field), joints 10 Hz, control 50 Hz -- the real policy runs on zero-order-held stale observations, so 50 Hz Stonefish odom is already faster than reality
- `finding/218` [subject: real-robot-deployment-vibration-differential-diagnosis-by-si] Real-robot deployment vibration: differential diagnosis by sim-to-real gap channel (which DR/noise probe to reach for) — Real-robot deployment vibration: differential diagnosis by sim-to-real gap channel (which DR/noise probe to reach for)
- `finding/220` [subject: relaxing-the-linear-evolution-constraint-improves-multi-step] Relaxing the linear-evolution constraint improves multi-step prediction in 10 of 10 offline configurations on the ALBC plant — Relaxing the linear-evolution constraint improves multi-step prediction in 10 of 10 offline configurations on the ALBC plant
- `finding/221` [subject: removing-fault-dr-from-the-teacher-recipe-has-a-measurable-s] Removing fault-DR from the teacher recipe has a measurable signature on the HEALTHY eval too: better nominal steady state, much worse hard-DR tail, and a wider thruster_util margin — Removing fault-DR from the teacher recipe has a measurable signature on the HEALTHY eval too: better nominal steady state, much worse hard-DR tail, and a wider thruster_util margin
- `finding/222` [subject: repeat-eval-instrument-noise-on-in-loop-latent-r2-0-027-at-h] Repeat-eval instrument noise on in-loop latent R2: 0.027 at hard, 0.22 at none — Repeat-eval instrument noise on in-loop latent R2: 0.027 at hard, 0.22 at none
- `finding/223` [subject: report-completeness-gate-must-run-before-writing-not-after] report completeness gate must run BEFORE writing, not after — report completeness gate must run BEFORE writing, not after
- `finding/224` [subject: retracted-the-2026-08-25-headline-that-100-hz-imu-observatio] RETRACTED: the 2026-08-25 headline that 100 Hz IMU observations closed the yaw divergence (240 -> 1.26 deg) was a radians-labelled-as-degrees error (57.3x) on top of a raw-IMU-frame read; true numbers are 240 -> 71.9 deg yaw and roll std 5.2 deg, so only accelerating divergence -> bounded drift (19 -> 1.15 deg/s) survives — RETRACTED: the 2026-08-25 headline that 100 Hz IMU observations closed the yaw divergence (240 -> 1.26 deg) was a radians-labelled-as-degrees error (57.3x) on top of a raw-IMU-frame read; true numbers are 240 -> 71.9 deg yaw and roll std 5.2 deg, so only accelerating divergence -> bounded drift (19 -> 1.15 deg/s) survives
- `finding/225` [subject: reward-absolute-scale-is-invariant-to-the-constrainttrpo-act] Reward absolute scale is invariant to the ConstraintTRPO actor; only term ratios matter (3 un-normalized leak paths) — Reward absolute scale is invariant to the ConstraintTRPO actor; only term ratios matter (3 un-normalized leak paths)
- `finding/226` [subject: reward-cost-parallel-structure-mostly-mirroring-two-real-cou] reward/cost parallel structure: mostly mirroring, two real couplings (value grad-clip over the union throttles reward critic; feasibility J_C uses GAE-return so constraint level inherits lambda_c + cost-critic bias) — reward/cost parallel structure: mostly mirroring, two real couplings (value grad-clip over the union throttles reward critic; feasibility J_C uses GAE-return so constraint level inherits lambda_c + cost-critic bias)
- `finding/228` [subject: reward-md-deep-dive-session-2026-07-11-4-review-fix-prompts-] reward.md deep-dive session 2026-07-11: 4 review/fix prompts queued in .sp/plans/ (§6 dt-scaling, §7 sigma-gate, §9 gotchas-triage pending; bias-reward DONE) — reward.md deep-dive session 2026-07-11: 4 review/fix prompts queued in .sp/plans/ (§6 dt-scaling, §7 sigma-gate, §9 gotchas-triage pending; bias-reward DONE)
- `finding/229` [subject: reward-penalty-terms-thruster-smoothness-bias-block-3-tempor] Reward penalty terms thruster/smoothness/bias block 3 temporal bands (magnitude/jerk/DC-offset); bias EMA makes a Markov reward see non-Markov drift — Reward penalty terms thruster/smoothness/bias block 3 temporal bands (magnitude/jerk/DC-offset); bias EMA makes a Markov reward see non-Markov drift
- `finding/231` [subject: roll-error-is-dc-bias-dominated-ss-jitter-ss-error-pitch-car] Roll error is DC-bias dominated (ss_jitter << ss_error); pitch carries a larger — Roll error is DC-bias dominated (ss_jitter << ss_error); pitch carries a larger
- `finding/232` [subject: roll-transient-is-worst-at-none-dr-and-improves-monotonicall] roll transient is WORST at none DR and improves monotonically as DR hardens (inverted, both runs) — roll transient is WORST at none DR and improves monotonically as DR hardens (inverted, both runs)
- `finding/233` [subject: rp-vel-settling-and-rp-rate-margins-decline-slowly-in-the-fi] `rp_vel_settling` and `rp_rate` margins decline slowly in the final third (9.154 — `rp_vel_settling` and `rp_rate` margins decline slowly in the final third (9.154
- `finding/235` [subject: saturation-is-iteration-clocked-not-env-clocked-the-referenc] Saturation is iteration-clocked, not env-clocked. The reference lineage saturate — Saturation is iteration-clocked, not env-clocked. The reference lineage saturate
- `finding/237` [subject: sequential-overnight-campaign-wall-clock-budget-not-crashes-] Autonomous campaign tail-run died from MISSING launch automation, not wall-clock — Autonomous campaign tail-run died from MISSING launch automation, not wall-clock
- `finding/239` [subject: sigma-decay-under-an-expanding-dr-curriculum-literature-verd] Sigma decay under an expanding DR curriculum: literature verdict and the projection-not-clamp correction — Sigma decay under an expanding DR curriculum: literature verdict and the projection-not-clamp correction
- `finding/240` [subject: sim-hydro-nominal-is-analytical-not-measured-imu-pressure-ca] sim hydro nominal is analytical (not measured); IMU+pressure can anchor rotation/heave but not surge/sway/TAM — sim hydro nominal is analytical (not measured); IMU+pressure can anchor rotation/heave but not surge/sway/TAM
- `finding/242` [subject: static-eval-plots-regenerate-sim-free-from-npz-reconstruct-s] static-eval plots regenerate sim-free from npz (reconstruct segment metadata) — static-eval plots regenerate sim-free from npz (reconstruct segment metadata)
- `finding/244` [subject: stepint400-does-not-forbid-a-slow-curriculum-ramp-its-expans] stepint400 does not forbid a slow curriculum ramp: its expansion budget was 2.40 KL against the 3.12 needed to saturate, so it tested under-saturation rather than the knob — stepint400 does not forbid a slow curriculum ramp: its expansion budget was 2.40 KL against the 3.12 needed to saturate, so it tested under-saturation rather than the knob
- `finding/245` [subject: stonefish-base-hull-effective-hydro-measured-2026-07-27-damp] Stonefish base-hull effective hydro MEASURED (2026-07-27): damping is the gap axis (yaw ~45-100x, pitch-linear ~10x, translational 3-8x under nominal); effective added inertia already matches; nominals recentered on marinelab exp/hydro-recenter — Stonefish base-hull effective hydro MEASURED (2026-07-27): damping is the gap axis (yaw ~45-100x, pitch-linear ~10x, translational 3-8x under nominal); effective added inertia already matches; nominals recentered on marinelab exp/hydro-recenter
- `finding/248` [subject: stonefish-servo-position-mode-bang-bangs-at-the-deployed-1-0] Stonefish Servo position mode bang-bangs at the deployed 1.0/1.0 per-step gains, and the 50 Hz odom aliased the chatter away while biasing base yaw rate +41 percent (fixed at 0.1/0.1, interim) — Stonefish Servo position mode bang-bangs at the deployed 1.0/1.0 per-step gains, and the 50 Hz odom aliased the chatter away while biasing base yaw rate +41 percent (fixed at 0.1/0.1, interim)
- `finding/249` [subject: stonefish-yaw-gap-claim-review-main-body-hydro-yaw-torque-st] Stonefish yaw-gap claim review: main-body hydro yaw torque structurally zero (symmetric added mass kills Munk); PhysX DOES model arm reaction; real gaps = buoy added-mass ~10x under, no arm-link hydro, no yaw-torque DR axis — Stonefish yaw-gap claim review: main-body hydro yaw torque structurally zero (symmetric added mass kills Munk); PhysX DOES model arm reaction; real gaps = buoy added-mass ~10x under, no arm-link hydro, no yaw-torque DR axis
- `finding/250` [subject: student-distillation-converges-to-a-residual-that-rules-out-] Student distillation converges to a residual that rules out latent multimodality (teacher-visited distribution only) — Student distillation converges to a residual that rules out latent multimodality (teacher-visited distribution only)
- `finding/251` [subject: student-distillation-roll-heavy-tail-is-a-teacher-policy-pro] Student distillation roll heavy-tail is a teacher-policy property (TCN==GRU), not a distillation artifact — Student distillation roll heavy-tail is a teacher-policy property (TCN==GRU), not a distillation artifact
- `finding/255` [subject: tam-vertical-single-motor-dual-esc-measured-2026-07-05] TAM vertical pair is one physical motor with dual-ESC wiring (measured 2026-07-05) — TAM vertical pair is one physical motor with dual-ESC wiring (measured 2026-07-05)
- `finding/256` [subject: teacher-and-student-checkpoints-live-at-different-depths-und] Teacher and student checkpoints live at DIFFERENT depths under the run tree: teacher writes train/model_N.pt, train_student.py writes train/models/student_N.pt -- a completion watcher copied from the teacher polls a path that will never exist — Teacher and student checkpoints live at DIFFERENT depths under the run tree: teacher writes train/model_N.pt, train_student.py writes train/models/student_N.pt -- a completion watcher copied from the teacher polls a path that will never exist
- `finding/257` [subject: teacher-cross-axis-correlation-collapses-none-hard-axis-deco] teacher: cross-axis correlation collapses none->hard (axis decorrelation) — teacher: cross-axis correlation collapses none->hard (axis decorrelation)
- `finding/258` [subject: teacher-dr-harder-doraemon-curriculum-froze-before-run-end-u] teacher dr_harder: DORAEMON curriculum froze before run end (unused headroom) — teacher dr_harder: DORAEMON curriculum froze before run end (unused headroom)
- `finding/259` [subject: teacher-dr-harder-yaw-is-the-only-heavy-tail-axis-roll-is-dc] teacher dr_harder: yaw is the ONLY heavy-tail axis, roll is DC-bias — teacher dr_harder: yaw is the ONLY heavy-tail axis, roll is DC-bias
- `finding/260` [subject: teacher-encoder-0-dead-latent-dims-keys-on-lateral-cog-cob-o] teacher encoder: 0 dead latent dims, keys on lateral CoG/CoB offsets — teacher encoder: 0 dead latent dims, keys on lateral CoG/CoB offsets
- `finding/261` [subject: teacher-hard-dr-cv-explodes-without-heavy-tail-dc-bias-dispe] teacher hard-DR: CV explodes without heavy-tail (DC-bias dispersion) — teacher hard-DR: CV explodes without heavy-tail (DC-bias dispersion)
- `finding/262` [subject: teacher-segmented-post-switch-roll-transient-jumps-under-har] teacher segmented: post-switch roll transient jumps under hard DR — teacher segmented: post-switch roll transient jumps under hard DR
- `finding/264` [subject: the-deployed-teacher-trained-with-control-delay-steps-0-0-wh] The deployed teacher trained with control_delay_steps (0,0) while the robot serves observations 1.2 to 4.7 control steps stale, and DORAEMON has no dim to cover it — The deployed teacher trained with control_delay_steps (0,0) while the robot serves observations 1.2 to 4.7 control steps stale, and DORAEMON has no dim to cover it
- `finding/265` [subject: the-dgx16k-teacher-s-latent-target-carries-about-half-the-si] The dgx16k teacher's latent target carries about half the signal-to-noise of E-int's, which is a candidate mechanism for C3 non-transfer measurable without any student — The dgx16k teacher's latent target carries about half the signal-to-noise of E-int's, which is a candidate mechanism for C3 non-transfer measurable without any student
- `finding/266` [subject: the-doraemon-feasibility-floor-sits-inside-this-config-s-see] The DORAEMON feasibility floor sits inside this config's seed distribution: two replicate seeds ended on opposite sides of alpha with nothing but the seed changed — The DORAEMON feasibility floor sits inside this config's seed distribution: two replicate seeds ended on opposite sides of alpha with nothing but the seed changed
- `finding/268` [subject: the-floor-cannot-be-the-ceiling-here-on-this-run-s-own-evide] The floor cannot be the ceiling here, on this run's own evidence: 5/8 dims were — The floor cannot be the ceiling here, on this run's own evidence: 5/8 dims were
- `finding/269` [subject: the-floored-set-is-a-lineage-constant-reproduced-exactly-mod] The floored set is a lineage constant, reproduced exactly. `model_13400` sigma i — The floored set is a lineage constant, reproduced exactly. `model_13400` sigma i
- `finding/270` [subject: the-frozen-actor-s-sensitivity-to-latent-error-is-strongly-l] The frozen actor's sensitivity to latent error is strongly level-dependent: hard crosses the control floor at a 4x smaller perturbation than none/soft/medium — The frozen actor's sensitivity to latent error is strongly level-dependent: hard crosses the control floor at a 4x smaller perturbation than none/soft/medium
- `finding/271` [subject: the-interim-report-s-entropy-section-leaned-on-5-8-dims-pinn] The interim report's entropy section leaned on "5/8 dims pinned at their floor" — The interim report's entropy section leaned on "5/8 dims pinned at their floor"
- `finding/272` [subject: the-invariance-is-measured-not-assumed-four-checkpoints-re-e] The invariance is measured, not assumed. Four checkpoints re-evaluated two days — The invariance is measured, not assumed. Four checkpoints re-evaluated two days
- `finding/273` [subject: the-mean-preserving-beta-clamp-silently-triples-concentratio] The mean-preserving Beta clamp silently triples concentration for nominal-0 DR dims (30 -> 100), and those 4 dims are exactly the ones that end under-expanded — The mean-preserving Beta clamp silently triples concentration for nominal-0 DR dims (30 -> 100), and those 4 dims are exactly the ones that end under-expanded
- `finding/274` [subject: the-obs76-teacher-is-a-real-better-controller-than-e-int-at-] The obs76 teacher is a REAL-better controller than E-int at hard DR, but none of its advantage survives distillation to any student — The obs76 teacher is a REAL-better controller than E-int at hard DR, but none of its advantage survives distillation to any student
- `finding/275` [subject: the-one-non-flat-constraint-signal-is-a-14-dip-in-constraint] The one non-flat constraint signal is a -14% dip in `Constraint/margin/thruster_ — The one non-flat constraint signal is a -14% dip in `Constraint/margin/thruster_
- `finding/276` [subject: the-performance-has-plateaued-verdict-was-a-sampling-artifac] The "performance has plateaued" verdict was a sampling artifact of a 4-point eva — The "performance has plateaued" verdict was a sampling artifact of a 4-point eva
- `finding/277` [subject: the-pre-registered-per-env-paired-comparison-cannot-be-execu] The pre-registered per-env paired comparison cannot be executed: _per_env_ss_stats hides its vector and a docstring-faithful reimplementation misses the published scalar by up to 3.6x the floor — The pre-registered per-env paired comparison cannot be executed: _per_env_ss_stats hides its vector and a docstring-faithful reimplementation misses the published scalar by up to 3.6x the floor
- `finding/278` [subject: the-pre-registered-question-does-4x-envs-buy-anything-is-ans] The pre-registered question — does 4x envs buy anything — is answered negatively — The pre-registered question — does 4x envs buy anything — is answered negatively
- `finding/279` [subject: the-recovery-is-real-and-monotone-at-the-sampled-points-ever] The recovery is real and monotone AT THE SAMPLED POINTS: every consecutive step — The recovery is real and monotone AT THE SAMPLED POINTS: every consecutive step
- `finding/280` [subject: the-shape-of-the-curve-between-samples-is-unknown-and-the-90] The SHAPE of the curve between samples is unknown and the 9000 excursion is not — The SHAPE of the curve between samples is unknown and the 9000 excursion is not
- `finding/281` [subject: thrust-deadband-0-075-is-inert-today-enable-thrust-curve-fal] thrust_deadband 0.075 is inert today (enable_thrust_curve false) and wrong for the day it is not: the board measures 0.16 half-span, asymmetric — thrust_deadband 0.075 is inert today (enable_thrust_curve false) and wrong for the day it is not: the board measures 0.16 half-span, asymmetric
- `finding/282` [subject: thruster-nonlinear-curve-t200-sim-to-real-off-by-default-dea] Thruster nonlinear curve (T200 sim-to-real): off-by-default deadband + signed-square toggle (d34debc) — Thruster nonlinear curve (T200 sim-to-real): off-by-default deadband + signed-square toggle (d34debc)
- `finding/283` [subject: thruster-static-gain-gap-stonefish-quadratic-rated-20-03-n-v] Thruster static gain gap: Stonefish quadratic rated 20.03 N vs Isaac linear 40 N per unit command; 2.0x at full command and 4.65x in the policy operating band, with zero DR coverage; rotor time constant IS aligned — Thruster static gain gap: Stonefish quadratic rated 20.03 N vs Isaac linear 40 N per unit command; 2.0x at full command and 4.65x in the policy operating band, with zero DR coverage; rotor time constant IS aligned
- `finding/284` [subject: thruster-util-is-the-binding-constrainttrpo-constraint-in-7-] thruster_util is the binding ConstraintTRPO constraint in 7 of 7 runs -- A5 loosened non-binding ones — thruster_util is the binding ConstraintTRPO constraint in 7 of 7 runs -- A5 loosened non-binding ones
- `finding/285` [subject: track-cmd-att-deg-logs-the-env-mean-of-command-not-the-comma] Track/cmd_att/*_deg logs the env-mean of |command|, not the command range (13.5 deg = uniform +-30 x 0.9) — Track/cmd_att/*_deg logs the env-mean of |command|, not the command range (13.5 deg = uniform +-30 x 0.9)
- `finding/286` [subject: tracking-kernel-exp-quad-tanh-dead-zone-diagnosis-correct-bu] Tracking kernel exp-quad-tanh: dead-zone diagnosis correct but fix is on wrong axis (yaw not att_rp); the exp+L1+tanh stack has no literature precedent — Tracking kernel exp-quad-tanh: dead-zone diagnosis correct but fix is on wrong axis (yaw not att_rp); the exp+L1+tanh stack has no literature precedent
- `finding/287` [subject: train-py-max-iterations-under-resume-is-iters-from-resume-no] train.py --max_iterations under --resume is iters-FROM-resume, not an absolute target (e3 got +10000 not +5000) — train.py --max_iterations under --resume is iters-FROM-resume, not an absolute target (e3 got +10000 not +5000)
- `finding/289` [subject: training-anomaly-thresholds-tb-tag-alert-table] training anomaly thresholds (TB tag alert table) — training anomaly thresholds (TB tag alert table)
- `finding/290` [subject: training-diagnosis-decision-tree] training diagnosis decision tree — training diagnosis decision tree
- `finding/291` [subject: training-log-analysis-engine-reference-adapter] training-log analysis engine (reference adapter) — training-log analysis engine (reference adapter)
- `finding/292` [subject: training-loss-latent-is-not-a-valid-corroborator-of-an-eval-] Training loss_latent is NOT a valid corroborator of an eval-side latent result: the widened-encoder arm moved them in opposite directions — Training loss_latent is NOT a valid corroborator of an eval-side latent result: the widened-encoder arm moved them in opposite directions
- `finding/293` [subject: uniform-only-dr-full-roster-9-params-doraemon-bypassing-payl] uniform-only DR full roster (9 params, DORAEMON-bypassing) + payload XY-radius vs Z curriculum split — uniform-only DR full roster (9 params, DORAEMON-bypassing) + payload XY-radius vs Z curriculum split
- `finding/295` [subject: what-actually-mis-scales-at-16384-envs-plant-controlled-the-] What actually mis-scales at 16384 envs, plant-controlled: the trust region does not, exploration does (sigma -11 to -29 percent) and the cost critic does (+29 percent) — What actually mis-scales at 16384 envs, plant-controlled: the trust region does not, exploration does (sigma -11 to -29 percent) and the cost critic does (+29 percent)
- `finding/296` [subject: where-is-arm-w-losing-the-8-points-of-return-per-dr-dimensio] Where is Arm W losing the 8 points of return: per-DR-dimension quintile decomposition (M3) — Where is Arm W losing the 8 points of return: per-DR-dimension quintile decomposition (M3)
- `finding/297` [subject: within-one-run-the-training-log-is-blind-to-eval-regressions] Within one run the training log is blind to eval regressions: a 34 percent none-level degradation moved every TB metric under 1 percent (bounds the cross-treatment reward-decomposition rule) — Within one run the training log is blind to eval regressions: a 34 percent none-level degradation moved every TB metric under 1 percent (bounds the cross-treatment reward-decomposition rule)
- `finding/305` [subject: retrain-simtoreal-2026-09] Vertical-TAM wrong-axis lead REFUTED by the artifact; decision/197 HARD-GATE re-scored: item 3 closed robot-side by measurement, item 2 reduces to an unmeasured moment magnitude, item 4 moment-arm still open — handoff/304 lead refuted: deployed_tam.json puts the vertical pair on +-x (3h/9h) with My, m0 tank probe gives pitch with roll unchanged, config->deployed column map byte-equal, T4 command frame = sim frame. finding/137 D-1 survives. HARD-GATE: 1 applied, 2 premise refuted (magnitude unmeasured), 3 closed robot-side (+102 deg, T2/T4), 4 max_thrust applied / moment-arm open. Incumbent fault_severity hit Beta(1,1) at iter 7748 (artifact read).
- `finding/308` [subject: retrain-simtoreal-2026-09] Literature R1-R5 re-verified against primary text: 10 supported, 3 partly, 1 contradicted (2512.13359 is not a UVMS and has no dead band) -- no plan decision moves — Re-verification 2026-09-03 of the 13 external sources cited by the frozen retrain plan. Full-text passages, not abstracts: R1-c (arXiv 2512.13359 fixed +/-5 N band) is contradicted -- the paper abstracts to body forces and defers actuators to deployment; R1-d (2508.19164) is adaptive control not RL but its failed deadband compensation is real; MMDR +90 pct should read nearly 100 pct moving distance; Gangapurwala Table I confirms 50 ms at 50 Hz vs 90 ms at 10 Hz. No decision in PLAN v3.1 changes; four text corrections owed to section 12.
- `finding/309` [subject: retrain-simtoreal-2026-09] Vertical TAM: the lever and the frame stay; the "3x gap" is RETRACTED (unit error -- the m0 probe was RAW, effective u=0.1176, ratio 0.67x at the DR floor), and R-1 (2026-09-03) found neither pre-registered law but a shifted origin -- the absolute coefficient now hinges on net buoyancy B, which is the next probe — CORRECTED 2026-09-03 (twice). (a) The 3x/0.31x vertical-moment gap is RETRACTED as a unit error: the 08-12 m0 probe used b1_channel_probe.py, which publishes RAW commands with the mixer bypassed, so raw 0.25 is effective u=0.1176 (undeadband D=0.15) -- the measured 0.455 N.m is 0.67x the linear nominal 0.682 N.m, at the coefficient-DR floor (0.7,1.3), and 5.7x the signed-square curve, not the other way round. (b) R-1 ran 2026-09-03 (vault finding/144): the PLAN section 7 settled-tilt protocol is UNEXECUTABLE on this robot -- m3 dead means m0 is the only vertical channel, reallocate() turns every command into net heave, and both + steps clamped at 0.205 m and both - steps at 0.890 m with doubling the thrust changing neither. Replacement readout (terminal descent rate, 9/9 steps, R2 0.988-0.996, free rise 0.0230 m/s pins B=c*v_rise^2 so drag cancels): the law fits NEITHER pre-registered option -- not curvature but a shifted origin, T ~ (u-delta) with delta ~ 0.11-0.14 linear above it (T=15.5*u^1.0 N). That residual deadband undercuts option (d) premise (finding/281 assumed the mixer fully inverts the ESC deadband). Absolute coefficient is undecided and thruster levels are no longer the lever: B=0.53 (free-rise fit) gives 0.39x, vault finding/136 B=1.07 gives 0.78-0.86x INSIDE the DR band -- a FIRST DIRECT measurement of net buoyancy B (it has never been weighed as an assembly) is now the deciding probe. Also identified: (m+m_a)=52.0 kg (added mass 41.7 on 10.3 dry), c=1000 N.s2/m2. Also retracted: rev1 +20.2 deg/s yaw was wall reaction. Frame, lever 0.145 m, 40 N constant, D-1 conclusion all stand.
- `finding/311` [subject: retrain-simtoreal-2026-09] G0-I met: thruster_dead_frac dead atom + exposure unit test, incumbent bit-identical at 0 (commit 4cef724, implementer agy) — G0-I gate met 2026-09-04: FaultInjectionCfg.thruster_dead_frac (default 0.0) + 5 lines in sample_thruster_health; no extra RNG draw at 0 so the incumbent sampler is bit-identical (asserted by test). tests/test_fault_sampler_exposure.py 4 passed (closed-form q=s*p0*d numbers within 0.5 pp at s=1 and s~U(0,1), all-healthy at s=0) + 42 existing fault/config tests. p0=0.15 is a launch-config value, not a code default. Implemented by agy (gemini) after the Claude agent hit 529 and codex returned 404; user: no codex for now. G0-C not queued.

## handoff
- `handoff/302` [subject: retrain-simtoreal-2026-09] From-scratch re-diagnosis before Phase-1 rewrite: latency reconciled, FTC lever is sampler-shape, gates need 2 seeds, J1 soft-only — Fresh primary-source pass: obs staleness closed by firmware so the open gap is the untrained action delay; disturbance/fault weakness is a DORAEMON clamp+budget artifact whose naive severity-widening already backfired (lever=sampler shape); every gate needs >=2 seeds; J1 has a soft cost but no hard rail. PLAN banner stands, nothing launched.
- `handoff/303` [subject: retrain-simtoreal-2026-09] retrain PLAN: two rounds merged + cross-vendor review BLOCK → O-1..O-4 open — Round1+Round2 merged into one PLAN; codex adversarial review returned BLOCK (11 defects, code-confirmed); corrections applied, 4 tensions O-1..O-4 left for the user; nothing launched.
- `handoff/304` [subject: retrain-simtoreal-2026-09] STOP: retrain PLAN frozen — failed its own decision/197 HARD-GATE; vertical TAM may be on the WRONG AXIS — PLAN frozen, not launchable: 2 of 4 decision/197 HARD-GATE items were silently missing (IMU 45deg frame entirely; vertical TAM redesign mis-framed). finding/255 correction says m0=3oclock m3=9oclock (two motors, left-right) so the sim My=+-0.145 pitch coupling may be on the wrong axis. Verify that FIRST.

## review
- `review/306` [subject: retrain-simtoreal-2026-09] Evidence ledger for retrain-simtoreal-2026-09: 31 claims of the frozen PLAN re-scored against both stores (444 posts) -- 9 corrections the rewrite must carry — Codex ground-2 sweep of vault (140) + marinelab (304) posts, session-judged. Frozen PLAN errors: thrust-ON bag 10-19-06 unanalyzed; no J2>pi guard exists; thruster_sign default is identity; 6-8% figure is the 5k arm not the incumbent (Beta(1,1) at 7748); thruster_util range 0.805-0.943; performance_lb 250 is not p25 (261.8); C3 non-transfer RETRACTED; DGX 5.41 not 5.56 s/iter; 1/30000 vs 0.09% are different events. Full vendor ledgers appended verbatim.
- `review/307` [subject: retrain-simtoreal-2026-09] Codex ground-4 review of retrain PLAN revision 3: BLOCK, 11 findings -- 8 confirmed and folded into revision 3.1 (R-1 identifiability, pair arithmetic, severity-integrated exposure, explicit gate thresholds, launch manifest), 2 rejected, 1 partly — Different-family adversarial review of PLAN v3. Most consequential: R-1 static tilt cannot separate lever from thrust coefficient and must not auto-edit the plant (now decision 9); P(m3,m4 dead) was 0.20/0.41 not 0.41/0.66 %; exposure must integrate severity U(0,1) (P>=2 dead 1.1-4.1 %); G0 thresholds were ungrounded; commit not pinned. agy failed 3x so the two-family gate is NOT met. Reviewer output verbatim inside.
- `review/310` [subject: retrain-simtoreal-2026-09] agy (Gemini 3.1 pro-high) ground-4 review of PLAN revision 3.1: 3 BLOCK / 3 MAJOR / 1 MINOR -- two findings duplicate finding/309 independently (40 N coefficient, linear-only R-1 readout), two accepted into 3.3, two rejected on config.py evidence, one partly; two-family gate met — Second vendor family. Files had to be INLINED (file-pointing prompts time out or return files-missing); 64 KB inline returned in 3m57s. Findings: axis-naming objection rejected (pitch = rotation about sim y, rotate_imu maps the IMU into that frame, m0 probe verified); fixed-health index objection rejected (config.py:130-137 orders sim channels as m0..m5); 40 N coefficient and linear R-1 readout confirmed and already fixed in 3.2 via finding/309; delay-row/G0-B contradiction and the G0-B [1.3x,2x) gap accepted into 3.3; deadband inertness asserted in G0-J. codex and agy did not overlap; both found the 50->40 N constant that finding/309 also found.
